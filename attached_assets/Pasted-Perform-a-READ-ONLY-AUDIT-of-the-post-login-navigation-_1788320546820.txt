Perform a **READ-ONLY AUDIT of the post-login navigation flow**.

Do NOT modify any files yet.

The expected behavior is:

`Successful Email Login/Verification`
→ `Clerk Session Finalized`
→ `Check Device Security`
→ `Biometric/Passcode Setup`
→ `Setup Successful`
→ `Home (/(tabs))`

For an existing user who already configured device security:

`Successful Login / Session Restore`
→ `Biometric/Passcode Unlock`
→ `Home (/(tabs))`

## TRACE THE CURRENT FLOW

Inspect:

* `artifacts/travel-land-app/app/login.tsx`
* `artifacts/travel-land-app/app/verify.tsx`
* `artifacts/travel-land-app/app/_layout.tsx`
* `artifacts/travel-land-app/app/biometric.tsx`
* `artifacts/travel-land-app/app/biometric-login.tsx`
* `artifacts/travel-land-app/context/AuthSecurityContext.tsx`
* `artifacts/travel-land-app/features/role/RoleContext.tsx`
* role routing files
* any splash/bootstrap routing involved

Trace exactly what happens after:

### Email/password success

`signIn`
→ `finalize`
→ Clerk `isSignedIn`
→ root navigation
→ device security check
→ role resolution
→ Home

### Email verification success

`verify`
→ Clerk verification
→ signup/sign-in finalization
→ Clerk session
→ root navigation
→ device security
→ Home

## IMPORTANT

Determine whether the current code actually does this:

`Successful authentication`
→ `Biometric setup`

or whether it currently does:

`Successful authentication`
→ `/(tabs)`

without waiting for device-security setup.

Also determine whether `/v1/me` or role resolution is blocking the navigation.

## CHECK THESE STATES

### STATE 1 — New user

Expected:

Login
→ successful authentication
→ biometric/device-security setup
→ user enables biometric/passcode
→ setup success
→ Home

### STATE 2 — User skips device security

If the current product design allows "Skip for now":

Login
→ authentication
→ device-security setup
→ Skip
→ Home

Do not remove the existing option unless the current requirements explicitly prohibit it.

### STATE 3 — Existing user with biometric enabled

Expected:

Session restored
→ biometric prompt
→ successful unlock
→ Home

### STATE 4 — Existing user with device security disabled

Expected:

Session restored
→ no biometric prompt
→ Home

### STATE 5 — SecureStore unavailable

Expected:

Authentication/session
→ SecureStore unavailable
→ recovery screen
→ remain locked

It must NOT go directly to Home.

## CHECK NAVIGATION CONDITIONS

Identify every condition that can prevent Home navigation.

Especially inspect:

* `isLoaded`
* `isSignedIn`
* `isUnlocked`
* `deviceAuthSetupComplete`
* `SECURE_STORAGE_UNAVAILABLE`
* `role`
* `roleLoading`
* `roleError`
* Clerk session state
* `/v1/me`
* pathname/navigation guards

Determine if there is any condition where the app:

* stays on Login
* stays on Verification
* stays on Biometric setup
* shows a blank screen
* waits indefinitely
* redirects back to Login

## CHECK LOGIC ORDER

The intended priority should be approximately:

1. Clerk loading
2. Authentication state
3. SecureStore availability
4. Device-security setup/unlock
5. Role resolution
6. Authenticated Home/role destination

Determine whether the existing `_layout.tsx` follows a safe equivalent order.

Do NOT change it yet.

## TESTS

Inspect existing tests for:

* successful login navigation
* verification navigation
* biometric setup
* biometric unlock
* session restoration
* role routing
* SecureStore recovery

Do NOT add tests yet.

## REQUIRED FINAL REPORT

### 1. CURRENT FLOW

Show the actual current flow as an arrow diagram.

### 2. EXPECTED FLOW

Show the expected flow:

`Login/Verification`
→ `Clerk Session`
→ `Device Security`
→ `Biometric Setup`
→ `Home`

### 3. FINDING

Choose:

**PASS — Current flow already reaches biometric setup and Home correctly**

**FAIL — Authentication succeeds but navigation/state prevents biometric setup or Home**

**UNVERIFIED — Cannot determine from current evidence**

### 4. EXACT BLOCKER

If FAIL, identify:

* exact file
* exact function/component
* relevant condition/state
* why Home or biometric setup is not reached

### 5. LIVE TEST

If the environment allows safe interaction, observe the actual post-login behavior.

Do NOT request or expose credentials.

If live login cannot be performed, clearly mark it NOT LIVE TESTED.

### 6. RECOMMENDED FIX

Give the smallest fix required.

Do NOT implement it.

### 7. FILES CHANGED

Must be:

**NONE**

This is a READ-ONLY audit.

Do not modify code, configuration, Clerk settings, database, secrets, or authentication behavior.
