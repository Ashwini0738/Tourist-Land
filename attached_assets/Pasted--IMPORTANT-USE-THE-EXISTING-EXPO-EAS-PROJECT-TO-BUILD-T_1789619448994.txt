# IMPORTANT: USE THE EXISTING EXPO EAS PROJECT TO BUILD THE LATEST SOURCE

We already have an existing Expo/EAS project:

https://expo.dev/accounts/ashwini0738s-team/projects/travel-land-app?tab=activity

The old Android APK was previously generated through this Expo/EAS project.

The current Replit workspace cannot perform a local Android/iOS native build because Android SDK/Java/Gradle/Xcode are not installed.

**Do NOT attempt another local native build.**

Instead, use the existing Expo/EAS project and trigger the appropriate **cloud EAS builds** for the CURRENT source.

---

# 1. IMPORTANT — DO NOT REWRITE AUTHENTICATION

The current source has already passed:

- Mobile tests: 116/116
- API Clerk tests: 3/3
- TypeScript: PASS
- Expo Doctor: 18/18
- Workflow validation: PASS
- Expo dependency validation: PASS
- Production Android JS bundle: PASS
- Production iOS JS bundle: PASS

The current authentication architecture is:

- Clerk Email OTP
- Clerk Phone OTP
- Clerk session
- `/v1/me`
- role loading
- biometric routing
- Home navigation

Do NOT rewrite the authentication system again.

Do NOT switch providers.

Do NOT reintroduce Supabase Auth.

Do NOT modify working authentication code unless a build actually exposes a real defect.

---

# 2. VERIFY EAS PROJECT CONNECTION

First inspect:

```text
app.json
app.config.js
app.config.ts
eas.json
package.json
```

Confirm the Expo project ID/project configuration points to the existing:

```text
travel-land-app
```

Expo account/team:

```text
ashwini0738s-team
```

Do not create a new Expo project.

Do not create a second project.

Do not change the existing project identity.

---

# 3. VERIFY EXPO/EAS AUTHENTICATION

There is already an `EXPO_TOKEN` configured.

Check whether the current environment can authenticate against EAS.

Run the equivalent EAS project/build inspection commands available in this environment.

If `eas` is not installed, determine whether it can safely be invoked using:

```text
npx eas-cli
```

Do NOT assume that the absence of a globally installed `eas` binary means EAS cloud builds are impossible.

The important distinction is:

```text
Local Android build
≠
EAS cloud build
```

We specifically need the EAS cloud build.

---

# 4. ANDROID APK CLOUD BUILD

Use the existing APK/internal distribution profile.

The Android package must remain:

```text
com.arohagroup.travelandland
```

Current:

```text
version: 1.0.0
versionCode: 3
```

Build an actual:

```text
.apk
```

NOT merely:

```text
.js
```

and NOT merely:

```text
.aab
```

The expected build profile should use an APK-compatible configuration such as:

```json
{
  "distribution": "internal"
}
```

or:

```json
{
  "android": {
    "buildType": "apk"
  }
}
```

Use the project's existing configuration if it is already correct.

Do not unnecessarily modify `eas.json`.

---

# 5. TRIGGER THE ANDROID EAS BUILD

Trigger the cloud build for the latest source.

Use the current source tree.

The build must include all latest changes:

- Clerk Email OTP
- Clerk Phone OTP
- biometric changes
- authentication fixes
- preview/runtime fixes
- Android callback intent-filter removal
- current navigation changes
- current `/v1/me` integration

Do NOT build an old commit.

Do NOT use the previous APK.

The build must be a NEW EAS build from the CURRENT source.

---

# 6. VERIFY THE ANDROID BUILD

After triggering the build:

Wait for the actual EAS build result if possible.

Report:

```text
EAS project
Build ID
Build profile
Platform
Version
Version code
Package name
Build status
Artifact URL
```

If the build succeeds, provide the actual APK artifact link.

Do NOT say:

> APK generated

unless EAS actually reports a successful APK artifact.

---

# 7. IOS CLOUD BUILD

Now use the same existing Expo/EAS project for iOS.

Bundle identifier:

```text
com.arohagroup.travelandland
```

Current:

```text
version: 1.0.0
buildNumber: 1
```

Use the existing iOS internal distribution profile if configured.

Trigger an EAS iOS build.

---

# 8. IOS SIGNING

If EAS asks for Apple Developer credentials/signing configuration:

Use the EAS-managed credential flow.

Do NOT put Apple private keys or certificates into source files.

If credentials are already configured, continue with the build.

If credentials are missing, report exactly what EAS requires.

Do not falsely report an IPA.

---

# 9. IMPORTANT IOS DISTRIBUTION DETAIL

For internal iOS distribution, the build may require registered device UDIDs/ad hoc provisioning.

If the existing profile is configured for internal distribution:

- inspect the current configuration
- determine whether devices are registered
- use the appropriate EAS device/provisioning workflow if available

If the user wants TestFlight/App Store distribution instead, use the appropriate production/App Store build profile.

Do not silently change the distribution method.

---

# 10. DO NOT CONFUSE BUNDLES WITH NATIVE BUILDS

These files:

```text
_expo/static/js/android/bundle.js
_expo/static/js/ios/bundle.js
```

are NOT the final mobile packages.

The required artifacts are:

Android:

```text
.apk
```

iOS:

```text
.ipa
```

or the corresponding EAS/TestFlight distribution artifact when the selected iOS distribution workflow does not expose a raw IPA directly.

---

# 11. VERIFY SOURCE VERSION IN THE BUILD

Before declaring success, verify that the EAS build corresponds to the current source.

The build should include the latest Clerk migration.

Do not accidentally build an old EAS/Expo artifact.

If possible, include a build message such as:

```text
Travel & Land — Clerk Email OTP + latest auth fixes
```

so the EAS activity page clearly identifies this build.

---

# 12. DO NOT CREATE A NEW EXPO PROJECT

This is critical.

Existing project:

```text
ashwini0738s-team/travel-land-app
```

Use it.

Do NOT:

- create another Expo project
- create another Android package ID
- create another iOS bundle ID
- create another EAS project
- upload the JS bundle as if it were an APK
- create a second application

---

# 13. FINAL REPORT

Return this exact information:

## Android

```text
Expo project:
Build ID:
Profile:
Status:
Package:
Version:
Version code:
Artifact:
```

## iOS

```text
Expo project:
Build ID:
Profile:
Status:
Bundle ID:
Version:
Build number:
Artifact:
```

## Source validation

```text
Clerk Email OTP:
Clerk Phone OTP:
Biometric:
Supabase Auth:
Tests:
Expo Doctor:
TypeScript:
```

---

# 14. FAILURE HANDLING

If Android EAS build cannot start, give the exact EAS error.

For example:

- invalid EXPO_TOKEN
- project not found
- project ownership mismatch
- EAS CLI unavailable
- build profile invalid
- missing credentials
- missing Android signing credentials
- EAS service unavailable

Do NOT simply say:

> Android SDK is missing.

That is only relevant to a LOCAL Android build.

We are specifically trying to use the EXISTING EAS CLOUD BUILD.

Similarly, for iOS, report the exact Apple/EAS signing requirement if the build cannot proceed.

---

# FINAL GOAL

Use the existing Expo project:

```text
ashwini0738s-team/travel-land-app
```

and build the CURRENT source through EAS cloud services.

Expected result:

```text
CURRENT REPLIT SOURCE
        ↓
EXISTING EXPO/EAS PROJECT
        ↓
       EAS
      /   \
     ↓     ↓
 Android  iOS
   APK     IPA/TestFlight build
```

Do not perform another authentication rewrite.

Do not create another Expo project.

Do not report success until the EAS build actually produces the native artifact.