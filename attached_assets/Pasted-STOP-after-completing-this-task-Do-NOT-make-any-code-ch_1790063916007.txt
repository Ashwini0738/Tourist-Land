STOP after completing this task. Do NOT make any code changes, documentation changes, commits, or configuration changes.

I need a READ-ONLY audit of ONLY the Clerk login flow.

Do not audit the entire application yet.

Inspect the actual current source code and give me the results directly in your response.

Check these 5 things only:

1. CLERK INITIALIZATION
- Where Clerk is initialized in the mobile app
- Which Clerk publishable key is actually used
- Whether development/production key selection is correct
- Whether any fake/demo auth bypass exists

2. CREATE ACCOUNT
Trace the exact current flow:
Create Account
→ enter email
→ send OTP
→ enter OTP
→ verify OTP
→ create Clerk session
→ navigate to Home

Tell me:
- exact files/functions involved
- whether each step is implemented
- any confirmed defect

3. SIGN IN
Trace the exact current flow:
Sign In
→ enter email
→ send OTP
→ enter OTP
→ verify OTP
→ create/restore Clerk session
→ call backend /me
→ navigate to Home

Tell me:
- exact files/functions involved
- whether each step is implemented
- any confirmed defect

4. OTP
Verify the actual Clerk SDK methods currently used.

Confirm whether the code uses the correct current methods for:
- sending email OTP
- verifying email OTP
- handling incorrect OTP
- handling expired OTP
- handling resend OTP
- handling already signed-in users

Also check whether the application contains any hardcoded demo OTP such as 123456 or any development-only authentication bypass.

5. SESSION
Check:
- how Clerk session is persisted
- what happens when the app is closed and reopened
- what happens when the session is expired
- what happens after logout
- whether the app incorrectly redirects to login while a valid Clerk session exists

IMPORTANT:
Do not change anything.

Do not create an audit markdown file.

Do not create a checkpoint task.

Do not continue into Razorpay, deployment, database, biometrics, Organizations, vendor/admin roles, or other application features.

RETURN THE RESULTS NOW using exactly this format:

## CLERK LOGIN AUDIT — STEP 1

### 1. Clerk Initialization
Status: PASS / FAIL / PARTIAL
Files:
Findings:

### 2. Create Account
Status: PASS / FAIL / PARTIAL
Flow:
Files:
Findings:

### 3. Sign In
Status: PASS / FAIL / PARTIAL
Flow:
Files:
Findings:

### 4. Email OTP
Status: PASS / FAIL / PARTIAL
SDK methods found:
Findings:

### 5. Session
Status: PASS / FAIL / PARTIAL
Files:
Findings:

### Confirmed Defects
1.
2.
3.

### Coverage Gaps
1.
2.

### Recommended Fixes
List ONLY fixes required for the Clerk login flow.

Do not give generic advice.

Do not say that you are going to perform the audit.

Actually perform the source inspection and return the findings in this response.