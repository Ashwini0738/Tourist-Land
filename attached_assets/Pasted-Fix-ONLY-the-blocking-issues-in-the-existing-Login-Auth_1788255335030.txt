Fix ONLY the **blocking issues in the existing Login/Authentication flow identified by the completed login audit**.

IMPORTANT:

* Do NOT implement Email OTP.
* Do NOT implement Phone OTP.
* Do NOT redesign the login screen.
* Do NOT migrate Clerk.
* Do NOT change Clerk provider/configuration.
* Do NOT create a custom authentication system.
* Do NOT change the database schema.
* Do NOT make unrelated application changes.
* Preserve all currently working authentication behavior.

The audit identified these priority issues:

1. UNHANDLED CLERK PROMISE REJECTIONS
   Files:

* `artifacts/travel-land-app/app/login.tsx`
* `artifacts/travel-land-app/app/verify.tsx`

Problem:
Clerk operations handle returned `{ error }` values but do not consistently catch thrown exceptions from network failures, SDK failures, or finalize failures.

Fix:

* Wrap all relevant async Clerk operations in proper `try/catch/finally`.
* Convert thrown Clerk/network errors into a safe user-visible error message.
* Never expose internal SDK details.
* Ensure loading states always reset in `finally`.
* Do not log passwords, OTPs, tokens, or secrets.

Verify these paths:

* email/password sign-in
* phone sign-in
* MFA/email verification
* signup verification
* signup finalization
* sign-in finalization

2. ROLE-RESOLUTION ERROR CAN LEAVE A BLANK SCREEN

File:

* `artifacts/travel-land-app/app/_layout.tsx`

Problem:
When `/v1/me` fails while resolving the user's role, the app can wait indefinitely without showing a recovery UI.

Fix:

* Add a proper authentication/role-loading error state.
* Show a clear user-friendly message.
* Provide a Retry action.
* Provide a safe option to return to Login/sign out if appropriate.
* Do not bypass role authorization.
* Do not assume a role when `/v1/me` fails.
* Do not route users into Traveller/Vendor/Admin screens without a verified role.

Preserve the existing role source:
`/v1/me` → RoleContext → role routing.

3. SIGNUP VERIFICATION ERROR HANDLING

File:

* `artifacts/travel-land-app/app/verify.tsx`

Problem:
Verification/finalization failures can become unhandled promise rejections.

Fix:

* Add proper try/catch handling.
* Add loading state.
* Display safe error messages.
* Ensure the user can retry.
* Do not create duplicate users.
* Do not alter the existing Clerk signup architecture.

4. SIGNUP RESEND SAFETY

Still in:

* `artifacts/travel-land-app/app/verify.tsx`

Improve only the error/loading behavior:

* Disable resend while request is in progress.
* Handle Clerk resend errors.
* Show a safe error message.
* Prevent accidental repeated requests.
* If a cooldown already exists, preserve it.
* Do not introduce a custom OTP system.

5. DO NOT FIX THESE YET

The audit also identified:

* Forgot-password flow
* Local email/password validation
* Keyboard-aware login layout
* Expo web cross-origin authentication
* SecureStore fail-open behavior
* Splash/session race
* Admin/vendor navigation mismatch
* Clerk/PostgreSQL synchronization improvements
* Phone-only signup
* Email OTP-first login

Do NOT touch these in this task.

We will handle them one by one after this fix is verified.

## TESTING REQUIRED

After making the fixes, run:

1. TypeScript check.
2. Existing auth/security tests.
3. Role-routing tests.
4. Any relevant login/verification tests.
5. Expo/Metro startup.
6. Open the Login screen.
7. Verify no new runtime errors appear.

If possible, test failure simulation using mocked Clerk responses for:

* rejected sign-in promise
* rejected verification promise
* rejected finalize promise
* role `/v1/me` failure
* resend failure

Do NOT request or store real passwords, OTPs, tokens, or secrets.

## REQUIRED FINAL REPORT

Return:

### FIXED

* Clerk exception handling: PASS/FAIL
* Verification exception handling: PASS/FAIL
* Resend handling: PASS/FAIL
* Role error recovery: PASS/FAIL

### REGRESSION CHECK

* Existing email/password login: PASS/FAIL/NOT LIVE TESTED
* Existing phone login: PASS/FAIL/NOT LIVE TESTED
* Existing signup: PASS/FAIL/NOT LIVE TESTED
* Existing email verification: PASS/FAIL/NOT LIVE TESTED
* Existing MFA: PASS/FAIL/NOT LIVE TESTED
* Session restoration: PASS/FAIL/NOT LIVE TESTED
* Traveller routing: PASS/FAIL
* Vendor routing: PASS/FAIL
* Admin routing: PASS/FAIL
* Biometric/passcode: PASS/FAIL
* Existing auth/security tests: PASS/FAIL
* TypeScript: PASS/FAIL
* Expo/Metro: PASS/FAIL

### FILES CHANGED

List every changed file and explain exactly why.

### REMAINING ISSUES

List the issues from the audit that were intentionally NOT changed.

IMPORTANT:
Do not claim a live login PASS unless an actual test account was available and the login was tested.
