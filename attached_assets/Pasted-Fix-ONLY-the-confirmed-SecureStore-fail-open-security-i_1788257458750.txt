Fix ONLY the confirmed **SecureStore fail-open security issue** in the existing mobile authentication/device-security flow.

IMPORTANT:

* Do NOT implement Email OTP.
* Do NOT implement Phone OTP.
* Do NOT modify Clerk configuration.
* Do NOT change email/password authentication.
* Do NOT change backend authentication.
* Do NOT change database schema.
* Do NOT change role authorization.
* Do NOT modify unrelated UI.
* Preserve the existing biometric/passcode architecture.

AUDIT FINDING:

File:
`artifacts/travel-land-app/context/AuthSecurityContext.tsx`

Problem:
If SecureStore fails while reading the user's device-security settings, the current catch block sets:

* `deviceAuthSetupComplete = true`
* `isUnlocked = true`

This is fail-open behavior.

A SecureStore failure must NEVER automatically unlock the application.

## REQUIRED FIX

Change the error behavior to fail CLOSED.

If SecureStore cannot be read:

1. Do NOT mark device authentication as configured.
2. Do NOT mark the app as unlocked.
3. Keep the protected application locked.
4. Show a recoverable user-facing state.
5. Allow the user to retry SecureStore access.
6. If retry continues to fail, provide a safe path to sign out/return to Login where appropriate.
7. Do not bypass Clerk authentication.
8. Do not silently disable biometric/passcode protection.

IMPORTANT SECURITY RULE:

SecureStore read failure ≠ "device security is not configured."

Treat it as:

`SECURE_STORAGE_UNAVAILABLE`

not as:

`DEVICE_SECURITY_DISABLED`

## PRESERVE EXISTING BEHAVIOR

The following must continue working:

### Device security disabled

If the user has explicitly skipped/disabled device security:

→ app may remain unlocked according to the existing design.

### Device security enabled

If biometric/passcode security is enabled:

→ app starts locked
→ existing biometric/passcode unlock flow is used.

### SecureStore failure

If settings cannot be read:

→ app remains locked
→ show recoverable error
→ Retry
→ safe logout option if necessary.

Do NOT fall back to unlocked state.

## ERROR UI

Create a simple existing-style recovery state.

Example behavior:

"Unable to verify device security"

"We couldn't securely access your device security settings."

[Retry]

[Sign out]

Do not expose raw SecureStore/native errors.

Do not log sensitive information.

## TESTS

Update/add tests for:

1. SecureStore successfully returns device-security settings.
2. Device security enabled → app starts locked.
3. Device security disabled → existing unlocked behavior remains.
4. SecureStore read failure → app remains locked.
5. SecureStore read failure → `isUnlocked` must NOT become true.
6. SecureStore read failure → `deviceAuthSetupComplete` must NOT become true.
7. Retry after SecureStore failure succeeds.
8. Retry after repeated failure continues to fail closed.
9. Sign-out from the recovery state works safely.
10. Existing biometric flow still works.
11. Existing passcode/device fallback still works.

Do not weaken the tests merely to make them pass.

## REGRESSION TESTING

Run:

* TypeScript
* Full mobile test suite
* Auth/security tests
* Biometric tests
* Role-routing tests
* Expo/Metro startup
* Existing login screen preview

Do NOT perform real biometric testing unless an actual device is connected.

Do not claim real-device biometric success based on mocked tests.

## REQUIRED FINAL REPORT

### SECURITY FIX

* SecureStore failure fail-closed: PASS/FAIL
* Protected state remains locked: PASS/FAIL
* Retry: PASS/FAIL
* Safe logout: PASS/FAIL

### REGRESSION

* Email/password login code path: PASS/FAIL/NOT LIVE TESTED
* Phone login code path: PASS/FAIL/NOT LIVE TESTED
* Signup: PASS/FAIL
* Email verification: PASS/FAIL
* MFA: PASS/FAIL
* Forgot password: PASS/FAIL
* Biometric/passcode: PASS/FAIL
* Role routing: PASS/FAIL
* TypeScript: PASS/FAIL
* Tests: PASS/FAIL
* Expo/Metro: PASS/FAIL

### TEST COUNTS

Report the exact number of suites/tests passed and failed.

### FILES CHANGED

List every file changed and explain why.

### REMAINING ISSUES

Do not fix any other audit issues in this task. List them separately.

IMPORTANT:
Do not implement Email OTP or Phone OTP in this task.
