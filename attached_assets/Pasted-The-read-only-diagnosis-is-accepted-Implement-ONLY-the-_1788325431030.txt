The read-only diagnosis is accepted. Implement ONLY the confirmed `session_exists` Sign In fix.

### Confirmed root cause

Installed Clerk versions:

* `@clerk/expo` 4.6.0
* `@clerk/clerk-js` 6.30.1
* `@clerk/react` 6.14.7
* `@clerk/shared` 4.30.1

The current login handler incorrectly assumes:

`signIn.password() → session_exists → signIn.existingSession.sessionId`

But in the reproduced failure:

`signIn.password()`
→ `session_exists`
→ `signIn.existingSession` is absent
→ no session ID
→ `setActive()` cannot run
→ app remains signed out
→ user sees the restart message

### Implement the smallest safe fix

Modify ONLY:

* `artifacts/travel-land-app/app/login.tsx`
* `artifacts/travel-land-app/tests/login-error-handling.test.tsx`

Use Clerk's supported session APIs for the installed SDK.

The Sign In logic must handle these cases separately:

#### Case 1 — Already signed in

If Clerk's `useAuth().isSignedIn` is already true when the user presses Sign In:

* Do NOT call `signIn.password()` again.
* Let the existing root-authoritative navigation flow proceed.
* Do not create another session.
* Do not show the `session_exists` error.

#### Case 2 — `session_exists`

When `signIn.password()` returns `session_exists`:

* Do NOT assume `signIn.existingSession` exists.
* Use the installed Clerk SDK's supported `useSessionList()` API to inspect registered client sessions.
* Find an actual usable session.
* If a usable session exists, activate it with the supported `setActive()` API.
* After successful activation, allow the existing root navigation state machine to handle:

  * biometric setup
  * biometric unlock
  * disabled security
  * role routing
  * Home

Do NOT directly navigate to `/(tabs)`.

#### Case 3 — No usable session

If:

* `session_exists` is returned,
* `existingSession` is unavailable,
* and `useSessionList()` contains no usable session,

show a clear user-visible error explaining that Clerk has reported an existing session but no usable local session is available.

Do NOT fabricate a session ID.

Do NOT repeatedly retry Sign In.

### Preserve the existing Sign In UX

Keep:

* visible `Signing in...` loading state
* disabled button while processing
* visible Clerk errors
* existing email validation
* existing password validation
* existing password recovery
* existing error handling

Do not clear the email/password merely because an error occurs.

### Critical requirement

Do NOT modify:

* `app/_layout.tsx`
* `AuthSecurityContext`
* biometric setup
* biometric unlock
* SecureStore
* RoleContext
* vendor/admin routing
* backend
* Clerk configuration
* Clerk package versions
* email verification
* SMS/phone authentication
* password recovery architecture

The previously implemented root-authoritative navigation architecture must remain unchanged.

### Tests

Update focused tests for:

1. Normal successful password Sign In.
2. Already-signed-in user does not start another password Sign In.
3. `session_exists` with a usable registered Clerk session activates that session.
4. `session_exists` without `existingSession` still works when `useSessionList()` provides a usable session.
5. `session_exists` with no usable registered session shows a visible error.
6. `setActive()` failure shows a visible error.
7. Loading state appears during the operation.
8. Sign In button is disabled while processing.
9. Existing validation tests still pass.
10. Existing password recovery tests still pass.
11. Successful authentication remains delegated to the root navigation state machine.

### Verification

Run:

* focused login tests
* full test suite
* TypeScript check
* Expo/Metro validation
* static Android/iOS bundle validation if already part of the project's existing verification command

### Final response must include

1. Exact root cause addressed.
2. Exact code change.
3. Exact files changed.
4. Focused test result.
5. Full test result.
6. TypeScript result.
7. Expo/build result.
8. Confirmation that no direct `/(tabs)` navigation was added.
9. Confirmation that no other auth/security files were changed.

Do not make any unrelated improvements.
