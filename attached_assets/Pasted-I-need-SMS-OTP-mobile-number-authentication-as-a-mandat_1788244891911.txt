I need SMS OTP mobile-number authentication as a mandatory requirement for the Travel & Land app.

Do NOT tell me simply that Replit-managed Clerk does not support it.

First investigate whether the existing Clerk application can be configured to support phone authentication using Clerk's official phone/SMS OTP APIs with our existing Expo integration.

Clerk's current documentation supports:

* Phone sign-up
* Phone sign-in
* SMS OTP
* @clerk/expo custom authentication flows
* Phone verification
* Email OTP
* Biometric sign-in for returning users

Use Clerk as the ONLY authentication provider.

DO NOT create:

* Custom OTP generation
* Custom SMS service
* Custom OTP database
* Duplicate authentication system
* Fake SMS verification

---

## REQUIRED INVESTIGATION

Check the existing project:

1. Current @clerk/expo version
2. Current Clerk authentication implementation
3. Whether the project uses custom Clerk flows or prebuilt authentication
4. Current Clerk publishable key configuration
5. Whether Native API is enabled
6. Whether the current Clerk instance supports phone sign-up/sign-in
7. Whether SMS authentication is available for this Clerk instance/plan
8. Whether India (+91) can be enabled in the SMS country allowlist
9. Whether the current Expo architecture supports the required phone OTP flow
10. Whether the existing biometric implementation can remain unchanged

DO NOT change application code yet.

---

## CLERK DASHBOARD REQUIREMENTS

Tell me exactly which Clerk Dashboard settings need to be enabled.

Expected areas to investigate:

User & Authentication
→ Phone

Enable where supported:

* Sign-up with phone
* Sign-in with phone
* Verify phone at sign-up

Also verify:

* Email authentication
* Email verification code
* Phone/SMS configuration
* SMS country allowlist
* India (+91)
* Native API if required
* Biometric authentication if already being used

If the current Replit-managed Clerk connection does not expose these settings, clearly explain what needs to be changed.

---

## REQUIRED AUTHENTICATION DESIGN

The application should eventually support:

### New user

Email OR Mobile Number

↓

OTP

↓

Clerk verification

↓

Device authentication enrollment

↓

Home

### Returning user

Fingerprint / Face ID / Device Lock

↓

Existing Clerk session

↓

Home

### Fallback

Email OR Mobile

↓

OTP

↓

Home

Do NOT require email OTP every time the app opens if the existing authenticated session/device credential is valid.

---

## IMPORTANT

If the current Replit-managed Clerk integration genuinely prevents enabling SMS authentication, DO NOT implement a workaround.

Instead report:

1. Exact Clerk limitation
2. Whether it is caused by Replit-managed configuration
3. Whether it is caused by Clerk plan
4. Whether it is caused by current Expo SDK/package version
5. Whether an external/independent Clerk application can be connected
6. Exact steps I need to perform manually
7. Estimated development changes required after SMS is enabled

STOP after this investigation.

Do not modify the authentication system until the configuration path is confirmed.
