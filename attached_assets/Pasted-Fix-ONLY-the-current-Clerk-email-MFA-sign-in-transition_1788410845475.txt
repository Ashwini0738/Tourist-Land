Fix ONLY the current Clerk email-MFA sign-in transition based on the latest read-only diagnosis.

CURRENT BUG:
Email + password → OTP → OTP verified successfully → `signIn.status === 'complete'` → `signIn.finalize()` succeeds → app returns to the normal Sign In form instead of going to biometric setup.

Then, because the first attempt already created a real Clerk session, pressing Sign In again produces:
“Clerk reported an existing session, but no usable local session is available for this account.”

ROOT CAUSE IDENTIFIED:
`verifySignInCode()` clears the MFA UI with:

`setSignInVerificationMethod(null)`

before confirming that the newly-created Clerk session is actually active/observable locally.

The installed `@clerk/expo` 4.6.0 implementation confirms that `signIn.finalize()` internally activates the session using `createdSessionId`, but the current React handler cannot rely on reading mutable Clerk state after `finalize()`.

IMPLEMENT ONLY THIS FIX:

Files allowed to change:

* `artifacts/travel-land-app/app/login.tsx`
* `artifacts/travel-land-app/tests/login-error-handling.test.tsx`

1. In `verifySignInCode()`:

   * Keep the existing `isSubmitting || isSignedIn` guard.
   * After `verifyEmailCode()` succeeds and `signIn.status === 'complete'`, immediately capture the real value of:
     `signIn.createdSessionId`
   * Capture it BEFORE calling `signIn.finalize()`.
   * Do not fabricate or generate any session ID.

2. Call:
   `await signIn.finalize({})`

   and continue checking its returned `{ error }`.

3. After successful finalization:

   * Ensure the captured real `createdSessionId` is activated/available using the installed Clerk API only if necessary.
   * Do not depend on reading `clerk.session` after finalization as proof of activation.
   * Do not use `useSessionList()` as a replacement for the newly-created session.
   * Do not invent a session ID.
   * Do not create a custom session/auth system.

4. MOST IMPORTANT:
   Do NOT call:
   `setSignInVerificationMethod(null)`
   until the successful finalization/session activation step has completed.

   If session activation/finalization fails:

   * keep the OTP/MFA screen visible;
   * show the existing safe error;
   * allow the user to retry;
   * do not return to the normal Sign In form.

5. Once Clerk finalization/session activation succeeds:

   * Clear the MFA verification state so the OTP screen cannot be submitted again.
   * Do NOT manually navigate to Home.
   * Do NOT manually navigate directly to `/(tabs)`.
   * Do NOT bypass biometric setup.

6. The existing root navigation state machine MUST remain responsible for the next transition:

   successful Clerk authentication
   → `isSignedIn === true`
   → security state
   → biometric setup (`/biometric`) for first-time setup
   → biometric completion
   → role resolution
   → Home / correct role destination.

7. Do NOT modify:

   * `app/_layout.tsx`
   * `features/auth/authenticatedNavigation.ts`
   * `context/AuthSecurityContext.tsx`
   * `context/RoleContext.tsx`
   * `app/biometric.tsx`
   * `app/biometric-login.tsx`
   * Clerk configuration
   * Clerk package versions
   * token cache
   * backend authentication.

TESTS:
Update/add focused tests that accurately model the real lifecycle:

* OTP verification succeeds and changes status to `complete`.
* `createdSessionId` is captured BEFORE `finalize()`.
* `finalize()` succeeds.
* The captured real session ID is used if activation is required.
* MFA UI is NOT cleared when finalization/activation fails.
* MFA UI IS cleared after successful finalization/activation.
* No fabricated session ID is ever used.
* A second verification cannot be submitted once `isSignedIn === true`.
* Successful authentication does NOT hard-code navigation to Home.
* Root navigation remains responsible for sending a newly authenticated user to biometric setup.

IMPORTANT:
Do not solve this by simply adding a delay, `setTimeout`, app restart, or navigating directly to Home. Fix the Clerk session lifecycle correctly.

After implementation run:

1. Focused MFA/login tests.
2. Full test suite.
3. TypeScript/typecheck.
4. Expo/Metro validation or the project's standard build validation.

Final response must include:

* exact files changed;
* exact fix implemented;
* focused test count/result;
* full test count/result;
* TypeScript result;
* Expo/build result;
* confirmation that biometric setup and root-authoritative navigation were preserved;
* confirmation that no unrelated files were changed.
