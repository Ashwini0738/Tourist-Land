Perform a READ-ONLY audit of the Travel & Land project. Do NOT modify any files, environment variables, Clerk configuration, Supabase configuration, authentication code, or build configuration.

I am seeing this runtime warning:

"Clerk: Clerk has been loaded with development keys. Development instances have strict usage limits and should not be used when deploying your application to production."

Determine exactly why the current app is loading Clerk development keys.

Check:

1. Which environment variable supplies the Clerk publishable key to the Expo/native app.
2. Whether the resolved key starts with pk_test_ or pk_live_.
3. Whether the Clerk key is coming from:
   - Replit Secrets / Environment Variables
   - app.config.js / app.config.ts
   - eas.json
   - .env files
   - Expo Constants
   - hardcoded source code
   - another configuration layer
4. Check BOTH Development and Production build environments if configured.
5. Identify every location where ClerkProvider or Clerk initialization receives the publishable key.
6. Confirm whether the current Expo Android APK build is intended to be:
   - development/testing, or
   - production/release.
7. Do NOT expose the full Clerk key in the output. Show only:
   - key type: pk_test or pk_live
   - source/location
   - environment name
   - whether it is appropriate for the current build.
8. Check whether the current Clerk instance is a Development or Production instance, if this can be determined from the project configuration.
9. Do not change or suppress the warning.

Also check whether changing the Clerk key now could affect the existing Travel & Land phone/SMS authentication flow or the existing user records.

Return a concise report with:
- Current Clerk key type
- Where it comes from
- Current build environment
- Whether this warning is expected
- Whether we should migrate to Clerk production now
- Any migration risks for the existing authentication flow
- Exact next step, without making changes.