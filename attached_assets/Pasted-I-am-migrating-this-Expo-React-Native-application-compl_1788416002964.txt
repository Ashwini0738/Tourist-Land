I am migrating this Expo/React Native application completely from Clerk Auth to Supabase Auth.

IMPORTANT: This is an AUDIT ONLY. Do NOT modify, delete, rename, or migrate any code yet.

Perform a complete repository-wide audit of every Clerk dependency and authentication-related implementation.

Find and categorize ALL of the following:

1. Clerk packages in package.json/package-lock and installed dependencies.
2. All imports from @clerk/* or any Clerk-related package.
3. Clerk providers such as ClerkProvider.
4. Clerk hooks such as:

   * useAuth
   * useUser
   * useSession
   * useSessionList
   * useSignIn
   * useSignUp
   * useClerk
   * any other Clerk hooks.
5. Clerk components such as:

   * SignedIn
   * SignedOut
   * SignIn
   * SignUp
   * UserButton
   * Protect
   * any other Clerk components.
6. Clerk authentication functions including:

   * signIn
   * signUp
   * prepareFirstFactor
   * attemptFirstFactor
   * prepareSecondFactor
   * attemptSecondFactor
   * MFA/OTP related Clerk APIs
   * session activation
   * session creation
   * session existence checks.
7. All Clerk-specific authentication state management.
8. All Clerk session persistence/restoration logic.
9. All biometric/device-unlock code that currently depends on Clerk.
10. All SecureStore/AsyncStorage keys related to Clerk or authentication.
11. All auth-related navigation guards and redirects that depend on Clerk.
12. All backend/API code that expects Clerk user IDs, session IDs, JWTs, or Clerk tokens.
13. All database records or schemas that reference Clerk user IDs.
14. All environment variables containing Clerk credentials/configuration.
15. All Replit secrets/environment configuration referencing Clerk.
16. All tests, mocks, fixtures, utilities, comments, documentation, and dead/duplicate code related to Clerk.
17. Identify duplicate authentication implementations or competing session ownership.
18. Identify any code that could continue running after Clerk is removed.

Create a dependency map with:

FILE
LOCATION
CLERK DEPENDENCY
PURPOSE
USED BY
MIGRATION ACTION
RISK

Also identify the authentication entry points and describe the current login flow from app startup → login → OTP/MFA → session → home.

Do not make any changes.

At the end provide:

A. Complete Clerk dependency inventory
B. Authentication flow currently implemented
C. Duplicate/dead authentication code
D. Files that must be changed
E. Files that can be deleted
F. Environment variables/secrets that must eventually be removed
G. Database/backend dependencies that must be migrated
H. Recommended migration order

STOP after the audit and wait for my next instruction.
