Fix ONLY the confirmed **Expo Web authenticated API request issue** identified in the read-only audit.

IMPORTANT:

* Do NOT implement Email OTP.
* Do NOT implement Phone OTP.
* Do NOT change Clerk configuration.
* Do NOT change environment variables or secrets.
* Do NOT change database schema.
* Do NOT change backend authentication.
* Do NOT change CORS unless testing proves an existing CORS problem.
* Do NOT change native Android/iOS authentication architecture.
* Do NOT change role authorization.
* Do NOT modify unrelated login/UI functionality.

## CONFIRMED ISSUE

The audit established:

Expo Web
→ Clerk Web session exists
→ Web disables the shared Clerk bearer-token getter
→ `customFetch` therefore sends no Authorization header
→ request is cross-origin
→ browser does not automatically send the required authentication
→ `/v1/me` can return `401 UNAUTHENTICATED`
→ RoleContext cannot resolve the user's role
→ app may return to Login.

The relevant existing code is:

`artifacts/travel-land-app/app/_layout.tsx`

Current behavior:

`setAuthTokenGetter(Platform.OS === 'web' ? null : () => getToken());`

This must be corrected.

## 1. MINIMUM FIX

Use the existing Clerk session's bearer token on Web as well.

The intended behavior should become equivalent to:

`setAuthTokenGetter(() => getToken());`

However, before changing it:

* Inspect the exact current implementation.
* Confirm the installed Clerk API supports `getToken()` in this context.
* Preserve the existing native behavior.
* Do not introduce a second authentication mechanism.

The resulting architecture should be:

### Native

Clerk session
→ getToken()
→ Authorization: Bearer
→ API
→ Clerk middleware
→ `/v1/me`

### Web

Clerk session
→ getToken()
→ Authorization: Bearer
→ cross-origin API
→ Clerk middleware
→ `/v1/me`
→ RoleContext
→ role routing

Do NOT switch the application to a cookie-based custom authentication system.

## 2. CUSTOM FETCH

Inspect:

`lib/api-client-react/src/custom-fetch.ts`

Confirm that the existing auth-token getter is correctly used to create:

`Authorization: Bearer <token>`

Do not expose or log the actual token.

If `customFetch` already supports the token getter correctly, do not unnecessarily modify it.

Only modify it if required for the Web bearer-token fix.

## 3. BACKEND

Do NOT modify backend code unless testing proves the existing backend cannot accept the bearer token.

The previous audit confirmed:

* Clerk middleware is installed.
* Authorization header is supported.
* CORS allows Authorization.
* CORS credentials are already configured.
* `requireAuth` correctly reads the Clerk user ID.

Preserve this.

## 4. WEB 401 HANDLING

Preserve the existing behavior:

Authenticated request
→ 401
→ safe Clerk sign-out
→ Login

Make sure the Web bearer-token change does not create:

* redirect loops
* repeated sign-outs
* infinite API retries
* blank screens

Do not redesign the 401 system unless an actual regression is found.

## 5. ADD TEST COVERAGE

Add focused tests for the Web bearer-token path.

Test:

### Test A — Web token getter

Mock Clerk `getToken()`.

Verify the Web authentication setup registers/uses the token getter.

### Test B — Authorization header

Mock:

`getToken() → test-token`

Verify the API request contains:

`Authorization: Bearer test-token`

Do not print real tokens.

### Test C — No token

If `getToken()` returns null/undefined:

* request should not send an invalid Authorization header
* application must not crash

### Test D — `/v1/me`

Mock an authenticated `/v1/me` response.

Verify the authenticated state can proceed to role resolution.

### Test E — Web 401

Mock `/v1/me → 401`.

Verify:

* Clerk sign-out is invoked safely
* user is returned to Login
* no infinite redirect/retry loop occurs

### Test F — Native regression

Verify native bearer authentication remains unchanged.

Do not remove existing native auth tests.

## 6. IMPORTANT — TEST THE ACTUAL WEB REQUEST PATH

If the environment supports browser interaction, perform a real Expo Web request after the fix.

Use only a safe development/test identity already available in the environment.

Do NOT request, display, or store credentials/tokens in chat.

If no live Clerk account is available, do not fabricate one.

In that case, use mocked/integration-level evidence and clearly mark live login as NOT TESTED.

## 7. VERIFY `/v1/me`

After the fix, confirm the request path is:

Web Clerk session
→ `getToken()`
→ `Authorization` header
→ API
→ Clerk middleware
→ `requireAuth`
→ `/v1/me`
→ local user
→ role
→ authenticated app

Do not claim successful real user authentication unless actually tested.

## 8. REGRESSION

Run:

* TypeScript
* Full mobile test suite
* Auth/security tests
* Role-routing tests
* Web-specific tests
* Expo/Metro startup
* Expo Web preview

Do not make unrelated fixes.

## REQUIRED FINAL REPORT

### FIX

* Web Clerk bearer token: PASS/FAIL
* Authorization header: PASS/FAIL
* `/v1/me` authentication path: PASS/FAIL
* Role resolution: PASS/FAIL
* Web 401 handling: PASS/FAIL

### REGRESSION

* Email/password: PASS/FAIL/NOT LIVE TESTED
* Phone login: PASS/FAIL/NOT LIVE TESTED
* Signup: PASS/FAIL
* Email verification: PASS/FAIL
* MFA: PASS/FAIL
* Forgot password: PASS/FAIL
* SecureStore security: PASS/FAIL
* Biometric/passcode: PASS/FAIL/NOT REAL DEVICE TESTED
* Role routing: PASS/FAIL
* Native bearer auth: PASS/FAIL
* TypeScript: PASS/FAIL
* Full tests: PASS/FAIL
* Expo/Metro: PASS/FAIL

### TEST COUNTS

Report exact:

* suites passed
* tests passed
* suites failed
* tests failed

### FILES CHANGED

List every changed file and why.

### LIVE TEST LIMITATION

Clearly state whether a real Clerk Web login was performed.

IMPORTANT:
Do not implement Email OTP or Phone OTP.
Do not fix any other audit issue.
