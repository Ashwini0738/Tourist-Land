Perform a final Razorpay production-readiness audit for the Travel & Land application.

DO NOT replace Razorpay, redesign the payment architecture, or make unrelated application changes.

The Razorpay migration is already implemented and automated verification currently shows:

- API tests: 95 passed
- Mobile tests: 154 passed
- API TypeScript: passed
- Mobile TypeScript: passed
- Admin TypeScript: passed
- Database TypeScript: passed
- Workflow validation: passed
- git diff --check: passed
- API starts successfully on port 8080
- Stripe is removed from the active payment flow

Audit the existing implementation and report only what is actually present.

### 1. Backend payment flow

Trace the complete flow:

booking
→ payment order creation
→ Razorpay checkout
→ payment response
→ backend verification
→ webhook
→ booking/payment state

Verify that all payment state changes happen safely on the server.

### 2. Security audit

Verify:

- RAZORPAY_KEY_SECRET never reaches the mobile bundle
- RAZORPAY_WEBHOOK_SECRET never reaches the mobile bundle
- only RAZORPAY_KEY_ID can be exposed to the mobile client
- Razorpay payment signatures are verified server-side
- webhook signatures use the raw request body
- amount is validated server-side
- currency is validated server-side
- booking ownership is validated
- provider order ID is validated
- duplicate provider references are rejected
- idempotency is enforced
- checkout locks prevent duplicate payment creation
- replay protection exists
- webhook events cannot modify unrelated bookings
- already-completed payments cannot be incorrectly transitioned
- database transactions protect payment/booking state transitions

Report any weakness with:
1. exact file
2. exact route/function
3. current behavior
4. required fix

Do not make changes automatically unless the issue is clearly a security or correctness defect.

### 3. Payment-state failure scenarios

Specifically inspect these cases:

A. Razorpay payment succeeds but mobile app crashes before receiving the result.

B. Razorpay payment succeeds but backend verification request never reaches the server.

C. Webhook arrives before the mobile verification request.

D. Mobile verification arrives before the webhook.

E. Same webhook arrives multiple times.

F. Old webhook arrives after a newer payment/order exists.

G. User double-taps the Pay button.

H. User tries to pay for another user's booking.

I. Razorpay reports a different amount or currency.

J. Payment is failed/cancelled.

For every scenario explain the resulting booking/payment state.

### 4. Webhook audit

Verify:

POST /api/razorpay/webhook

Check:

- raw body handling
- x-razorpay-signature verification
- event validation
- booking metadata validation
- provider order ID validation
- amount validation
- currency validation
- duplicate event handling
- payment transition rules
- notification deduplication
- old/mismatched order handling

### 5. Mobile audit

Verify the Expo app:

- does not contain Razorpay secret credentials
- uses only the publishable Razorpay key
- does not attempt native Razorpay checkout in Expo Go
- clearly handles Expo Go/native-build limitation
- uses react-native-razorpay only in a native build
- handles success
- handles failure
- handles cancellation
- handles missing network
- handles app interruption

### 6. Environment audit

Verify that:

RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET

are documented correctly and that production/test environments cannot accidentally mix credentials.

Confirm that Stripe credentials are no longer required anywhere in the active payment startup path.

Do not remove historical Stripe payment records.

### 7. Database audit

Verify payment records have appropriate protection against:

- duplicate provider payment IDs
- duplicate provider order IDs
- duplicate webhook processing
- invalid payment state transitions

Confirm historical payment records remain readable.

### 8. Sandbox readiness

Do not claim that a real Razorpay payment has been tested.

Instead, tell me exactly what I must configure manually in Razorpay Test Mode:

- API keys
- webhook URL
- webhook secret
- required webhook events
- any dashboard settings

Then give me the exact EAS build command required for Android testing.

### 9. Final report

Return exactly these sections:

1. CURRENT PAYMENT ARCHITECTURE
2. SECURITY STATUS
3. PAYMENT STATE MACHINE
4. WEBHOOK STATUS
5. MOBILE STATUS
6. DATABASE STATUS
7. SANDBOX CONFIGURATION REQUIRED
8. MANUAL TEST PLAN
9. PRODUCTION BLOCKERS
10. SAFE TO PROCEED / NOT SAFE TO PROCEED

Do not claim production readiness solely from automated tests.

Do not claim a real Razorpay transaction was tested unless an actual Razorpay sandbox transaction was performed successfully.