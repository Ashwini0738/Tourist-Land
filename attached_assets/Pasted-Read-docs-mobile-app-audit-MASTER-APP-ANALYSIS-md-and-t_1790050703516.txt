Read /docs/mobile-app-audit/MASTER_APP_ANALYSIS.md and the current source code.

We are now moving from audit to controlled implementation.

IMPORTANT:
Do not rebuild existing functionality.
Do not change working features unnecessarily.
Do not introduce new architecture unless required.
Do not implement phone OTP or Clerk Organizations yet.
Do not implement wallet, real maps, or new major features yet.

For this task, ONLY work on the security and foundation issues identified as P0/P1 in the audit:

1. Restrict credentialed CORS to explicit approved origins.
2. Review and strengthen demo authentication boundaries so demo authentication can never be enabled in a production build.
3. Add appropriate rate limiting / abuse protection for the identified OTP and public application/status endpoints.
4. Review application-status lookup for enumeration risk and propose/implement a safer approach.
5. Review API input validation and standardize the highest-risk routes.
6. Review payment provider reference/idempotency protection.
7. Do NOT change Clerk authentication flow.
8. Do NOT add phone OTP.
9. Do NOT add organizations.
10. Do NOT change the existing customer/vendor/admin role model.
11. Do NOT change booking business logic yet.
12. Do NOT change database schema unless absolutely required for these security fixes.

Before modifying anything:

A. Inspect the current implementation.
B. Identify the exact files/routes that require changes.
C. Explain the planned changes.
D. Implement only the necessary changes.
E. Run the existing automated tests.
F. Add focused tests for every security change.
G. Run TypeScript/type checks and relevant API tests.
H. Report exactly what changed.

Security requirements:

- Never expose secrets.
- Never print tokens or credentials.
- Keep development/preview behavior working.
- Production must reject demo authentication/configuration.
- Do not weaken existing Clerk authentication.
- Keep customer/vendor/admin authorization intact.
- Preserve existing API contracts unless a security fix absolutely requires a change.

At the end provide:

1. Files changed
2. Security issues fixed
3. Tests added
4. Tests passed
5. Any remaining security risks
6. Anything that requires my decision before proceeding

Do not proceed to booking/payment/supplier changes in this task.