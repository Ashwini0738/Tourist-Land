## FIX ONLY — EXPO PREVIEW TUNNEL FAILURE

The actual preview failure has now been identified.

The Expo app successfully reaches:

`Starting project`
`React Compiler enabled`
`Starting Metro Bundler`

and then fails at:

`CommandError: failed to start tunnel`
`remote gone away`

Therefore this is an **Expo/ngrok tunnel startup failure**, not a Supabase authentication or Step 6A code failure.

### IMPORTANT SCOPE

Do NOT:

* modify Supabase authentication
* modify Clerk authentication
* modify Step 6A
* create identity-linking code
* start Step 7
* modify mobile authentication logic
* modify database schema
* modify API authentication
* change password recovery
* change biometric behavior
* change application screens

### 1. Diagnose the tunnel failure

Inspect the current Expo/Replit workflow configuration and determine why:

`pnpm exec expo start --tunnel --port $PORT`

is failing with:

`failed to start tunnel`
`remote gone away`

Check:

* ngrok availability/status
* Expo CLI tunnel behavior
* Replit network restrictions
* current Expo SDK compatibility with the installed CLI
* whether the tunnel process is being terminated
* whether the configured `$PORT` is compatible with Expo tunnel mode
* whether stale Expo/Metro/ngrok processes are running
* whether the failure is transient or reproducible

Do not make changes until the cause is identified.

### 2. Preserve the existing external-development authentication requirements

The current workflow intentionally requires:

`TRAVEL_LAND_AUTH_TARGET=external-development`

and:

`TRAVEL_LAND_DEV_CLERK_PUBLISHABLE_KEY`

Do NOT remove these checks.

Do NOT replace the Clerk development key.

Do NOT change authentication configuration merely to make preview start.

### 3. Try the least invasive recovery first

Before changing configuration:

* terminate stale Expo/Metro/ngrok processes if present
* restart the affected workflow
* retry the same Expo command
* verify whether the tunnel comes up

If the same `remote gone away` error occurs, continue diagnosis.

### 4. Determine whether tunnel mode is actually required

Inspect how this project is currently accessed from:

* Replit Preview
* Expo Go
* physical Android device
* development browser/web preview

Do not assume tunnel mode can simply be removed.

If Replit Preview can operate through LAN/localhost while the physical device requires a tunnel, clearly distinguish those cases.

### 5. If ngrok/tunnel is the only problem

If Metro starts successfully but the only failure is the ngrok tunnel, use the smallest safe workflow adjustment that allows the development app to run without the failing tunnel, ONLY if it is compatible with the existing Replit/Expo development setup.

Do not alter application source code.

Do not alter authentication logic.

Do not remove required environment checks.

If a tunnel is required for the user's physical device, do not silently switch to a mode that makes the device unable to connect. Report the limitation instead.

### 6. Do not upgrade/downgrade Expo packages blindly

Do NOT change:

* Expo SDK
* React Native version
* Expo Router
* React version
* Metro version

unless the logs prove a version incompatibility is the direct cause.

No package upgrade should be performed merely because ngrok failed.

### 7. Validation

After the minimum fix:

* start the Expo workflow
* confirm Metro starts
* confirm the tunnel/connectivity mechanism starts successfully OR clearly document the supported non-tunnel mode
* confirm the Preview no longer crashes immediately
* confirm the app bundle can be served
* run mobile typecheck
* run relevant existing mobile tests
* run `git diff --check`

Also verify that there are NO changes to:

* Supabase auth
* Clerk auth
* Step 6A
* Step 7
* API auth
* database schema

### STOP CONDITIONS

If ngrok itself is unavailable or Replit's network prevents the tunnel from starting, do NOT modify application code to work around it.

Report the infrastructure limitation and the exact recommended development connection mode.

If the tunnel starts successfully after retry, make no unnecessary code/config changes.

### Final report

Return:

1. Exact root cause
2. Whether ngrok/tunnel was the failure
3. Whether Metro itself was healthy
4. What was changed, if anything
5. Whether the Preview now starts
6. Mobile test/typecheck results
7. Confirmation that authentication code was untouched
8. Confirmation that Step 6A was untouched
9. Confirmation that Step 7 was not started
