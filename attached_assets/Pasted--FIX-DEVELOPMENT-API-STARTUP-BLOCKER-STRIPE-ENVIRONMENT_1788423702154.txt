## FIX DEVELOPMENT API STARTUP BLOCKER — STRIPE ENVIRONMENT ONLY

Step 6A real-device validation is currently BLOCKED because the API workflow cannot remain running.

The API successfully completed database migrations, then stopped with:

`Error: Stripe is not connected to this environment.`

This is an environment/startup dependency issue and is unrelated to the Step 6A identity-linking implementation.

### STRICT SCOPE

Fix ONLY the existing development-environment Stripe startup dependency so the API can start and remain running.

Do NOT:

* modify Step 6A identity-linking logic
* modify Clerk authentication
* modify Supabase authentication
* modify phone/SMS OTP
* modify MFA
* modify biometrics
* modify password recovery
* modify database ownership
* modify `users.id`
* modify `users.clerk_user_id`
* modify `users.auth_user_id`
* start Step 7
* redesign Stripe
* connect a production Stripe account
* add production credentials
* bypass authentication/security checks

### 1. Diagnose first

Inspect the API startup path and determine exactly why it requires Stripe to be connected.

Identify:

* the startup check that throws `Stripe is not connected to this environment`
* which environment variable/configuration is missing
* whether this is an existing development-only requirement
* whether the API previously had a supported development/test configuration
* whether Stripe is required for ALL API startup or only payment-related routes

Do not change anything until the exact startup dependency is identified.

### 2. Use development/test configuration only

If the project already supports a development/test Stripe configuration, restore/use that existing mechanism.

Do NOT request or add real production Stripe credentials.

Do NOT hard-code any secret.

Do NOT commit secrets to the repository.

If a Stripe test/sandbox credential is required and the project has no existing safe mechanism for providing it, STOP and report the exact missing environment variable/connection requirement instead of inventing a workaround.

### 3. Preserve payment behavior

Do not modify:

* Stripe service implementation
* payment logic
* checkout logic
* wallet logic
* webhook behavior
* production configuration

unless the startup diagnosis proves that a minimal development configuration change is required.

### 4. Validation

After the minimum environment/configuration fix:

1. Restart the API workflow.
2. Confirm the API remains running.
3. Confirm database migrations remain successful.
4. Confirm the existing API health/readiness endpoint works if one exists.
5. Run API typecheck.
6. Run the existing API tests.
7. Run the Step 6A identity-linking tests again.
8. Run `git diff --check`.

Confirm that the Step 6A implementation is unchanged.

### IMPORTANT

If the only way to start the API requires a Stripe connection that cannot be provided in this environment, DO NOT modify application code.

Instead report:

* exact missing Stripe requirement
* exact environment/connection setting needed
* whether a Stripe test/sandbox connection is sufficient
* whether any code changes are unnecessary

### FINAL REPORT

Return:

1. Exact root cause of API startup failure.
2. Exact environment/configuration change made, if any.
3. Whether API remains running.
4. Database migration status.
5. API test results.
6. Step 6A test results.
7. Confirmation that authentication code was untouched.
8. Confirmation that Step 6A was untouched.
9. Confirmation that Step 7 was NOT started.
10. Any remaining requirement before real-device validation can begin.
