I want to create an Android APK of the existing Travel & Land React Native / Expo mobile app for CLIENT TESTING.

IMPORTANT:
- Do not rewrite or migrate the existing authentication architecture.
- Do not change Supabase, Clerk, Stripe, API, database, maps, or other business logic.
- Do not remove or disable any existing functionality.
- Do not modify production configuration unnecessarily.
- First inspect the existing project and report what is already configured.
- Make only the minimum safe changes required for an Android preview APK build.

TASK:

1. Inspect the project:
   - package.json
   - app.json / app.config.js / app.config.ts
   - eas.json if present
   - Expo SDK/version
   - React Native version
   - existing Android package/application ID
   - existing Expo/EAS configuration
   - environment-variable usage
   - Supabase redirect/auth configuration
   - any existing build scripts

2. Determine whether this is currently an Expo managed/prebuild project and whether EAS Build can be used.

3. If EAS is not configured:
   - install/configure the appropriate EAS CLI configuration
   - create/update eas.json
   - create a preview build profile specifically for client testing
   - Android preview build must produce APK, not AAB

Expected preview configuration should be equivalent to:

{
  "build": {
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    }
  }
}

Do not overwrite an existing eas.json blindly. Preserve existing profiles and merge safely.

4. Verify that the Android package name is valid and stable.
   If one already exists, preserve it.
   Do NOT randomly change the package name.

5. Verify app name, version and Android versionCode.
   Do not unnecessarily change the current production version.
   If versionCode needs to be incremented for the test build, explain why before changing it.

6. Check all environment variables required by the mobile app.
   Especially verify the variables required for:
   - Supabase
   - API/backend
   - authentication
   - maps
   - Stripe/payment test configuration
   - any other runtime services

Do NOT expose secret values in the output.
Do NOT put service-role keys, private keys, secret API keys, or other sensitive credentials into source code.

7. Check Expo/Supabase authentication redirect configuration.
   The app must not accidentally use localhost URLs in the standalone APK.
   Do not invent a redirect URL.
   Report exactly what the current app expects and what needs to be configured.

8. Run safe validation before building:
   - TypeScript check
   - existing tests
   - Expo configuration validation
   - dependency/configuration checks
   - ensure there are no obvious build blockers

9. If the project is ready, start an EAS Android preview build using:

eas build --platform android --profile preview

10. If EAS login/account/project configuration is required, STOP and clearly tell me exactly what I need to do. Do not create fake credentials or bypass authentication.

11. After the build is submitted, provide:
   - build status
   - EAS build URL
   - APK download URL if available
   - package name
   - app version
   - versionCode
   - build profile used
   - any required client installation instructions

12. Do NOT claim the APK was successfully created unless the EAS build actually completes successfully.

13. If the build fails:
   - identify the exact failure
   - do not randomly modify multiple files
   - make only the minimum correction
   - rerun validation
   - retry the build only when the cause is understood

FINAL REPORT FORMAT:

A. Project/build readiness
B. Existing Expo/EAS configuration
C. Changes made
D. Environment/auth configuration status
E. Validation results
F. APK build status
G. APK download link
H. Client installation instructions
I. Known limitations/issues