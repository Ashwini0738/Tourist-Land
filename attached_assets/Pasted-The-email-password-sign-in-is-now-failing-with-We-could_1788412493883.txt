The email/password sign-in is now failing with:

“We could not complete sign in. Please try again”

I need you to **reproduce this failure in the running app, inspect the actual runtime/console logs, identify the exact Clerk error, and then fix the root cause.**

DO NOT make another broad authentication rewrite.

### REQUIRED PROCESS

1. Start the Expo/mobile app.
2. Reproduce the issue by selecting **Sign in with Email & Password** and submitting the login.
3. Immediately inspect the runtime/console logs generated by that exact attempt.
4. Trace the failure through the complete authentication sequence:

Email/password submit
→ existing-session cleanup
→ Clerk sign-in creation
→ password verification
→ MFA/OTP if required
→ sign-in finalization
→ finalized session retrieval
→ session activation
→ Home navigation

5. Identify the EXACT operation that fails.

Report the actual Clerk:

* error code
* error name
* status
* message/longMessage
* failing operation

Do not replace the actual error with the generic:
“We could not complete sign in.”

### IMPORTANT

Check whether the new session cleanup introduced by the previous fix is itself causing the failure.

Specifically verify:

* Does session cleanup throw an exception?
* Does cleanup invalidate the Clerk client/sign-in state?
* Does `signIn.create()` fail after cleanup?
* Does password verification succeed?
* Does MFA start correctly?
* Does finalization succeed?
* Does Clerk return a different session ID after finalization?
* Does `setActive()` fail?
* Does the app fail while retrieving the finalized session?
* Does navigation happen before the active session is available?

### FIX THE ROOT CAUSE

Once the exact failure is identified, make the **smallest possible fix**.

Preserve the authentication architecture already implemented:

EMAIL/PASSWORD:
fresh login → safely clear stale previous session → new Clerk sign-in → MFA if required → finalize → activate NEW session → Home

BIOMETRIC:
reuse valid existing Clerk session → Home

Do NOT:

* replace Clerk
* create custom authentication
* remove MFA/OTP
* globally sign out on app startup
* modify biometric authentication
* modify unrelated application code
* hide the actual Clerk error
* add unnecessary retries

### VERY IMPORTANT TEST

Do not rely only on mocked Jest tests.

After fixing the code, perform an actual runtime test of:

1. Fresh email/password login with no existing session.
2. Email/password login when an old session exists.
3. Email/password + MFA if applicable.
4. Biometric login with an existing valid session.

The actual app must successfully reach Home after valid email/password authentication.

### FINAL REPORT

Before saying the issue is fixed, provide:

* Exact root cause found from runtime logs
* Exact Clerk error code
* Exact operation that failed
* Why the previous implementation caused it
* Files changed
* Minimal fix applied
* Runtime test result
* Automated test result
* Confirmation that biometric login was not broken

Do not report “fixed” based only on TypeScript/build/tests. The actual email/password login must be reproduced successfully in the running app.
