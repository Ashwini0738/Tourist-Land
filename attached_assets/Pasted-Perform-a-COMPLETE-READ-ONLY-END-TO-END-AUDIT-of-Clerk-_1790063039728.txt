Perform a COMPLETE READ-ONLY END-TO-END AUDIT of Clerk authentication for the Travel & Land mobile application and API.

IMPORTANT:
- DO NOT modify any files.
- DO NOT change dependencies.
- DO NOT change Clerk configuration.
- DO NOT change authentication code.
- DO NOT create Organizations.
- DO NOT create organization provisioning.
- DO NOT change database schema.
- DO NOT deploy.
- DO NOT add/change secrets.
- DO NOT configure Razorpay.

The purpose is to identify every issue that could cause login, OTP verification, session persistence, backend authentication, deep linking, logout, or biometric login problems.

The intended authentication architecture is:

USER
 ↓
Travel & Land mobile app
 ↓
Clerk authentication
 ↓
Email OTP verification
 ↓
Clerk session
 ↓
Travel & Land API
 ↓
Backend validates Clerk session
 ↓
Local application user record
 ↓
Local role: user / vendor / admin
 ↓
Home/dashboard

Organizations must NOT be required.

==================================================
1. CURRENT CLERK ARCHITECTURE
==================================================

Inspect the complete authentication implementation.

Report:

- Clerk SDK/package and version
- Expo/React Native integration
- Clerk provider/root configuration
- Clerk publishable key source
- development vs production key handling
- authentication target/environment handling
- Clerk secret key usage on backend
- Clerk middleware/authentication mechanism
- session token handling
- current-user endpoint
- `/v1/me` or equivalent endpoint
- local user provisioning/synchronization
- role resolution
- logout implementation
- session restoration on app startup

Identify every authentication-related file.

Do NOT modify anything.

==================================================
2. REGISTRATION FLOW
==================================================

Trace the complete Create Account flow from the UI to Clerk.

Expected flow:

Create Account
→ enter email
→ send OTP
→ receive OTP
→ enter OTP
→ Clerk verifies email
→ Clerk session created
→ local application user created/synchronized
→ user role assigned
→ authenticated home screen

Verify:

- exact Clerk API/method used to create the account
- exact OTP send method
- exact OTP verification method
- what happens after successful verification
- whether a Clerk session is definitely created
- whether the app waits for the session before calling the backend
- whether `/v1/me` is called
- whether local user creation is idempotent
- what happens if the network fails after Clerk verification
- what happens if the app closes after OTP verification
- what happens if the user enters an incorrect OTP
- resend behavior
- OTP expiry handling
- rate-limit handling
- duplicate email handling

Identify any race conditions.

==================================================
3. SIGN-IN FLOW
==================================================

Trace:

Sign In
→ email
→ send OTP
→ enter OTP
→ Clerk verification
→ session
→ backend authentication
→ `/v1/me`
→ Home

Verify the exact implementation.

Check specifically for:

- stale Clerk session
- session not available immediately after OTP verification
- API request occurring before session is ready
- missing Authorization header
- malformed Bearer token
- expired session
- session refresh
- API 401 handling
- redirect loops
- infinite loading
- incorrect navigation after successful login

==================================================
4. OTP EMAIL DELIVERY
==================================================

Determine exactly how OTP email is delivered.

Verify:

- Clerk email verification configuration
- email verification strategy
- whether OTP is enabled
- whether email verification is required
- whether the app uses Clerk's hosted/email infrastructure
- whether a custom email provider is involved
- whether development and production email behavior differs
- whether the current test environment can actually send OTP emails

DO NOT send an OTP.

Do NOT expose any email credentials.

Report what must be configured in the Clerk dashboard for reliable OTP delivery.

==================================================
5. PRODUCTION VS DEVELOPMENT CLERK
==================================================

Audit:

Development:
- publishable key
- secret key
- auth target
- demo authentication
- development URLs

Production:
- publishable key
- secret key
- auth target
- allowed domains
- redirects
- deep links

Determine whether the app can accidentally use development Clerk keys in a production/release build.

Verify the existing production guard behavior.

==================================================
6. MOBILE DEEP LINKING
==================================================

Inspect:

- app scheme
- Expo configuration
- Clerk redirect configuration
- sign-in return flow
- email verification return flow
- any OAuth/deep-link handling
- Android intent filters
- iOS URL schemes
- development URLs
- production URLs

Current intended scheme:

travel-land-app

Determine whether Clerk actually requires a redirect/deep-link for the current email OTP flow.

If it does not, say so clearly.

If it does, provide the exact configuration required.

==================================================
7. SESSION PERSISTENCE
==================================================

Audit what happens when:

A. user signs in
B. user closes app
C. user reopens app
D. phone restarts
E. network is unavailable temporarily
F. session expires
G. Clerk refreshes the session
H. user logs out
I. user reinstalls the app

Determine whether session persistence is handled by Clerk's supported React Native mechanism.

Check whether secure storage/keychain/keystore is correctly used.

Do NOT change anything.

==================================================
8. BACKEND AUTHENTICATION
==================================================

Trace an authenticated request from mobile to API.

Verify:

Mobile
→ Clerk session
→ Authorization Bearer token
→ API
→ Clerk token verification
→ Clerk user ID
→ local database user
→ local role
→ authorization

Check:

- token validation
- issuer
- audience
- expiry
- signature verification
- Clerk user ID extraction
- local user lookup
- automatic user creation if appropriate
- role lookup
- admin authorization
- vendor authorization
- unauthorized response
- expired-session response

Verify that the backend NEVER trusts a user ID supplied by the mobile client.

==================================================
9. /v1/me
==================================================

Audit the current `/v1/me` endpoint.

Verify:

- authentication required
- Clerk identity used
- local user lookup
- user creation/synchronization
- role returned
- vendor status returned
- admin status returned
- no client-supplied identity is trusted
- response is safe
- behavior when Clerk user does not exist locally
- behavior when local user exists but Clerk identity changed

==================================================
10. ROLES
==================================================

Verify the intended model:

user
vendor
admin

Confirm:

- roles are stored locally
- role assignment does not depend on Clerk Organizations
- normal users cannot make themselves vendor/admin
- vendor approval is server-side
- admin access is server-side
- mobile cannot simply send `{role:"admin"}` to obtain admin access

==================================================
11. CLERK ORGANIZATIONS
==================================================

Perform a full search for:

- organization
- organizationId
- organization_id
- createOrganization
- getOrganization
- organization provisioning
- choose organization
- active organization
- membership
- org role

Determine whether ANY active login path still depends on Clerk Organizations.

Expected result:

Organizations should NOT be required anywhere for:

- sign up
- sign in
- OTP
- session creation
- `/v1/me`
- user role
- vendor role
- admin role

If any organization dependency remains, identify the exact file and flow.

Do NOT remove it yet.

==================================================
12. LOGOUT
==================================================

Audit:

- Clerk signOut
- local state clearing
- navigation reset
- API cache clearing
- biometric state
- session restoration after logout

Verify that after logout:

- Clerk session is gone
- user cannot access authenticated screens
- API requests return 401
- reopening app does not automatically restore the logged-out session
- biometric login does not bypass explicit logout

==================================================
13. BIOMETRIC LOGIN
==================================================

Audit the existing biometric/device-unlock flow.

Expected behavior:

FIRST successful Clerk login
→ ask user whether to enable biometric/device unlock
→ user enables it
→ app securely stores the necessary local credential/session reference
→ next app launch
→ biometric/device authentication
→ restore/use valid Clerk session
→ Home

Verify:

- Face ID
- fingerprint
- device PIN/passcode where supported by the existing implementation
- secure storage
- behavior when biometric authentication fails
- behavior when biometric is disabled
- behavior when device lock changes
- behavior after logout
- behavior after app reinstall
- behavior when Clerk session has expired

IMPORTANT:
Do not store the Clerk secret key or any sensitive server secret on the device.

Do not assume biometric authentication itself replaces Clerk authentication.

==================================================
14. ERROR HANDLING
==================================================

Identify all user-facing authentication errors.

Check:

- invalid email
- invalid OTP
- expired OTP
- resend OTP
- too many attempts
- email not received
- network failure
- Clerk unavailable
- session expired
- API unavailable
- backend 401
- backend 403
- account already exists
- account creation failure
- logout failure

Identify misleading or technically incorrect messages.

==================================================
15. SECURITY
==================================================

Verify:

- no Clerk secret in mobile bundle
- no database credential in mobile bundle
- no Razorpay secret in mobile bundle
- no hardcoded production credentials
- no fake OTP in production
- no demo authentication in production
- no client-controlled role escalation
- no client-controlled Clerk user ID
- no bypass of Clerk verification
- no organization workaround
- no local-only authentication pretending to be Clerk authentication

==================================================
16. TESTS
==================================================

Inspect existing authentication tests.

Report tests for:

- registration
- OTP
- sign-in
- session creation
- session restoration
- `/v1/me`
- backend authorization
- roles
- logout
- expired sessions
- invalid tokens
- biometric flow
- organization dependency

Identify missing tests.

DO NOT modify tests.

==================================================
17. FINAL REPORT

Return the result in exactly this structure:

### A. Current authentication architecture

### B. Complete registration flow

### C. Complete sign-in flow

### D. OTP/email configuration

### E. Session persistence

### F. Backend authentication

### G. /v1/me

### H. User/vendor/admin roles

### I. Clerk Organizations dependency

### J. Logout

### K. Biometric/device unlock

### L. Deep linking

### M. Security findings

### N. Existing authentication tests

### O. BLOCKERS

### P. NON-BLOCKING ISSUES

### Q. REQUIRED FIXES

For every issue, include:

- Severity: Critical / High / Medium / Low
- File
- Current behavior
- Why it can cause login problems
- Recommended fix
- Test required after fixing

IMPORTANT:

Do not make any changes during this task.

I want the complete audit first. After receiving the report, we will fix the authentication system ONE issue at a time and run tests after each logical fix.