Do a READ-ONLY diagnosis of the CURRENT email/password + email OTP sign-in flow. Do NOT modify any files yet.

This is the exact new behavior after the previous fix:

1. Start a completely new app session.
2. Tap Sign In.
3. Enter valid email + password.
4. Password sign-in succeeds and Clerk asks for an email OTP.
5. Enter the OTP.
6. Tap “Verify and continue”.
7. Instead of going to biometric setup/unlock and then Home, the app returns to the Sign In page.
8. Tap Sign In again.
9. It shows:

“Clerk reported an existing session, but no usable local session is available for this account. Please sign out of the existing account and try again.”

The issue is reproducible.

IMPORTANT: Diagnose the CURRENT code after the previous MFA fix. Do not assume the previous diagnosis is still sufficient.

Trace the complete runtime/source flow:

Email/password
→ `signIn.password()`
→ MFA requirement
→ email OTP send
→ `signIn.mfa.verifyEmailCode()`
→ `signIn.finalize()`
→ session creation/activation
→ Clerk `isSignedIn`
→ AuthSecurityContext
→ biometric setup/unlock
→ RoleContext
→ root navigation
→ Home.

Inspect only the relevant current implementation and tests, especially:

* `artifacts/travel-land-app/app/login.tsx`
* `artifacts/travel-land-app/app/_layout.tsx`
* `artifacts/travel-land-app/features/auth/authenticatedNavigation.ts`
* `artifacts/travel-land-app/context/AuthSecurityContext.tsx`
* `artifacts/travel-land-app/context/RoleContext.tsx`
* `artifacts/travel-land-app/app/biometric.tsx`
* `artifacts/travel-land-app/app/biometric-login.tsx`
* Clerk provider/session initialization
* Clerk token/session cache
* `tests/login-error-handling.test.tsx`
* relevant authenticated navigation tests
* any current session-management helper.

Specifically determine:

A. Does `verifySignInCode()` successfully verify the OTP?

B. Does `signIn.status` become `complete`?

C. Does `signIn.finalize()` execute?

D. What does the CURRENT `finalize()` call return?

* `undefined`
* `{ error: null }`
* `{ error: ... }`
* another result.

E. After finalization, what session information is actually available?
Check the installed Clerk SDK's real API contract. Do NOT guess property names.

F. Is a real Clerk session created?

G. Is that session made ACTIVE?

H. Does `useAuth().isSignedIn` become true after OTP verification?

I. Does `useSessionList()` contain the session?

J. If a session exists but is not active, explain why the current code fails to activate it.

K. Does the root `_layout.tsx` receive `isSignedIn === true` after OTP verification?

L. If `isSignedIn` becomes true, does the root navigation state machine attempt `/biometric`?

M. If it does not attempt `/biometric`, identify the exact blocking state:

* Clerk loading
* security state not ready
* SecureStore
* biometric state
* role state
* route guard
* another condition.

N. Why does the app return to the Sign In page after successful OTP verification?

O. Why does the NEXT Sign In attempt produce:
“existing session, but no usable local session is available for this account”?

P. Determine whether the current `useSessionList()`/`setActive()` logic introduced by the previous fix is incorrectly rejecting or failing to activate the newly-created session.

Q. Determine whether the app is accidentally treating a successfully finalized session as an “existing session” on the next login attempt because the active session was never established.

R. Check whether the previous fix added any condition that can cause the login page to remain mounted or reappear after successful MFA.

Do NOT change:

* Clerk configuration
* Clerk package versions
* SecureStore architecture
* biometric architecture
* RoleContext
* backend authentication
* root navigation architecture
* unrelated signup/email verification.

Do NOT add temporary production behavior or hard-coded session IDs.

If runtime logging is absolutely necessary, first explain what cannot be proven statically. Do not expose tokens, passwords, OTPs, session tokens, or other secrets.

At the end provide:

1. EXACT ROOT CAUSE.
2. Exact first-tap sequence with the current code.
3. Exact session state immediately after OTP/finalize.
4. Why the app returns to Sign In.
5. Why the second Sign In produces the “no usable local session” error.
6. Smallest safe implementation fix.
7. Exact files that need modification.
8. Exact tests that must be added/updated.
9. Confirm that NO files were changed during this diagnosis.

Do not implement anything yet. I will review the diagnosis before authorizing the fix.
