Perform a READ-ONLY production deployment readiness audit for the Travel & Land application.

IMPORTANT:
- Do NOT modify any files.
- Do NOT install or upgrade dependencies.
- Do NOT configure Razorpay.
- Do NOT add or change secrets.
- Do NOT deploy.
- Do NOT change Clerk authentication.
- Do NOT change database schema.
- Do NOT change payment logic.
- Do NOT change booking logic.
- Do NOT make any code changes.

The purpose of this task is only to determine whether the application is ready for the next phase: deployment and Razorpay Test Mode configuration.

Check the current application and report the following.

## 1. Application architecture

Confirm:
- mobile application technology
- API/server technology
- database technology
- authentication provider
- payment provider
- API port
- mobile package/bundle identifiers
- current Expo SDK
- Node.js version expected by the project

## 2. Production API

Verify:
- API starts successfully
- production API build succeeds
- production start command
- API port configuration
- whether the API listens on the correct host/interface for deployment
- whether any localhost-only URLs remain in production configuration
- whether any development-only URLs are required
- whether CORS configuration is production-safe
- whether health-check endpoint exists
- health-check URL/path
- whether API errors expose sensitive information

## 3. Environment variables and secrets

Inspect environment-variable usage WITHOUT revealing secret values.

Create a table:

| Variable | Required in production? | Secret? | Currently configured? | Purpose |
|---|---|---|---|---|

Include:
- Clerk variables
- database variables
- Razorpay variables
- session/security variables
- API/public URL variables
- mobile/public variables
- any other production-critical variables

IMPORTANT:
Never print actual secret values.

Also identify:
- variables that currently have development/test defaults
- variables that must be changed before production
- variables that should NEVER be bundled into the mobile application

## 4. Database

Verify:
- production database connection method
- migrations
- whether migrations run automatically or manually
- whether the production database schema is current
- whether database credentials are server-only
- whether connection pooling is configured appropriately
- whether destructive migration/reset behavior exists

Do not modify the database.

## 5. Clerk authentication

Verify:
- production Clerk configuration
- development vs production key handling
- whether production guards exist
- whether Clerk secrets remain server-side
- whether authentication works independently of organizations
- whether user/vendor/admin roles are enforced server-side
- whether any localhost callback/redirect URLs remain
- whether mobile deep-link configuration needs production setup

Do not change authentication.

## 6. Razorpay

Do NOT configure Razorpay.

Only inspect the existing code and report:

- Razorpay API integration status
- required environment variables
- mobile public key requirement
- server-only secret requirements
- webhook endpoint path
- whether webhook requires a public HTTPS URL
- whether deployment is required before webhook configuration
- whether any hardcoded Razorpay credentials exist
- whether Test/Live mode separation is enforced or configuration-dependent

Do not expose any existing secret values.

## 7. Mobile application

Verify:
- Android package ID
- iOS bundle ID
- Expo/EAS configuration
- development/preview/production build profiles
- whether Razorpay requires a native build
- whether Expo Go can still be used for non-payment development
- whether Android APK/AAB production builds are configured
- whether iOS production build configuration exists
- required EAS secrets/credentials
- whether app API URL currently points to localhost, Replit preview, or another development URL

## 8. Deployment

Determine:

A. Can the API be deployed now?

B. What exact configuration is missing?

C. What public HTTPS URL will be needed?

D. What deployment platform/configuration is currently intended by this Replit project?

E. What command/workflow will start the production API?

F. Will deployment automatically provide a stable public HTTPS URL?

Do NOT actually deploy.

## 9. Security

Confirm the latest security state based on the work already completed.

Report:
- SAST status
- HoundDog status
- dependency audit status
- remaining UUID findings
- whether remaining UUID findings affect production runtime or only Expo development/build tooling
- any other production-blocking security findings

Do not rerun destructive or modifying security operations.

## 10. Production blockers

Create three lists:

### BLOCKING
Things that must be fixed before deployment.

### REQUIRED BEFORE RAZORPAY TESTING
Things that must be completed before performing a real Razorpay Test Mode payment.

### CAN BE DEFERRED
Non-blocking items that can safely be handled later.

## 11. Exact next sequence

At the end, provide the safest step-by-step sequence from the current state.

Example:

1. Deployment preparation
2. Add required production secrets
3. Deploy API
4. Verify public HTTPS API
5. Configure Razorpay Test Mode
6. Configure webhook
7. Build EAS development APK
8. Install APK
9. Perform Razorpay sandbox payment
10. Verify booking/payment/webhook
11. Production readiness review
12. Live Mode preparation

Only include steps that are actually required based on your audit.

FINAL REQUIREMENT:

This is a READ-ONLY audit.

Do not make any changes.
Do not deploy.
Do not configure Razorpay.
Do not expose secrets.

Return the audit clearly with:
- Current status
- Blocking issues
- Required configuration
- Deferred issues
- Exact next step