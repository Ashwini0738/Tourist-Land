We have completed the security hardening pass.

Current verification status:
- 92 API tests passed
- TypeScript typecheck passed
- Workflow validation passed
- API restarted successfully
- API listening on port 8080
- git diff --check is clean
- Existing booking/payment security protections must remain intact

IMPORTANT PAYMENT CHANGE:

The application currently initializes Stripe, but this project is being developed for an India-based business. Stripe new accounts in India are currently invite-only, so DO NOT require a Stripe account or Stripe credentials for the application to function.

Replace the current Stripe-specific payment implementation with Razorpay as the primary payment provider.

Do NOT simply delete Stripe code. Refactor the payment architecture so the application can support another provider in the future without rewriting booking/payment logic.

OBJECTIVES

1. RAZORPAY AS PRIMARY PAYMENT PROVIDER

Implement Razorpay for the existing payment/checkout flow.

The expected customer flow is:

Mobile App
→ Create booking/payment request
→ Backend creates Razorpay Order
→ Backend returns order information to mobile app
→ Mobile app opens Razorpay checkout
→ Customer pays using supported payment methods
→ Razorpay returns payment details
→ Backend verifies the Razorpay payment signature
→ Backend confirms/captures the booking/payment
→ Webhook provides authoritative payment event
→ Booking/payment status is updated safely

Support the payment methods available to the Razorpay account, including Indian methods such as:
- UPI
- Cards
- Net Banking
- Other enabled Razorpay methods

Do not hard-code assumptions about payment methods that are unavailable for the merchant account.

2. PAYMENT PROVIDER ABSTRACTION

Create a clean provider abstraction.

For example, conceptually:

PaymentProvider
- createOrder()
- verifyPayment()
- capturePayment() if required
- refundPayment()
- getPaymentStatus()
- handleWebhook()

Implement:

RazorpayPaymentProvider

Do not couple booking business logic directly to Razorpay SDK calls.

The booking service should work with the payment provider interface.

3. REMOVE STRIPE AS A REQUIRED DEPENDENCY

Search the complete repository for:

stripe
Stripe
STRIPE_
stripeClient
paymentIntent
checkout session
Stripe webhook
Stripe secret

Identify every usage.

Remove Stripe from the active payment flow.

Do not require:
STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET

The application must start successfully without Stripe credentials.

If Stripe is retained for future optional support, isolate it behind the payment-provider abstraction and make it completely optional.

The default provider must be Razorpay.

4. RAZORPAY ENVIRONMENT VARIABLES

Use environment variables/secrets only.

Expected configuration should be similar to:

RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET

Never expose RAZORPAY_KEY_SECRET or webhook secrets to the mobile application.

Only the public Razorpay key ID may be sent to the client when required by the SDK.

Update .env.example with safe placeholder values and comments.

Do NOT put real credentials into source code.

5. MOBILE APPLICATION

Check the existing React Native / Expo payment implementation.

Implement the Razorpay checkout flow appropriate for the current Expo/React Native architecture.

Before adding a package, inspect the existing project configuration and determine whether the Razorpay React Native SDK is compatible with the current Expo/EAS setup.

If a native module requires an Expo development build rather than Expo Go, document this clearly and configure the project correctly for EAS builds.

Do not introduce a web-only payment implementation for the native mobile app.

6. BACKEND ORDER CREATION

The backend must create the Razorpay order.

Never trust the mobile app for:
- final payable amount
- currency
- booking price
- discount amount
- tax amount
- booking ownership
- payment status

The backend must calculate/validate the payable amount from trusted booking data.

The mobile app should send only the booking/payment request information necessary to identify the booking.

7. PAYMENT VERIFICATION

Implement server-side verification of the Razorpay payment signature.

Never mark a booking as paid merely because the mobile application reports:

payment_success

The backend must verify the payment information.

Validate:
- Razorpay order ID
- Razorpay payment ID
- signature
- booking/customer relationship
- expected amount
- expected currency
- payment state

Reject mismatched or replayed payment information.

8. WEBHOOK SECURITY

Implement Razorpay webhook handling.

Verify the webhook signature using the Razorpay webhook secret.

Do not trust arbitrary webhook payloads.

Handle duplicate webhook delivery safely.

Webhook processing must be idempotent.

If the same webhook/event is received multiple times, it must not:
- create duplicate bookings
- mark payment multiple times
- create duplicate transactions
- trigger duplicate confirmation actions

9. EXISTING PAYMENT SECURITY MUST REMAIN

Do NOT remove or weaken the existing:

- checkout locks
- payment claims
- idempotency
- replay protection
- booking ownership checks
- server-side amount validation
- payment status validation
- transaction consistency

Preserve the existing security model while changing only the payment provider.

10. REFUNDS

Inspect the existing refund functionality.

Implement Razorpay refund support if the current application exposes refunds.

Refunds must be initiated by the backend only.

Validate:
- authenticated user/admin authorization
- original payment
- refundable amount
- refund amount
- current payment state

Prevent duplicate refund processing.

11. PAYMENT DATABASE MODEL

Do not change the database schema unless absolutely necessary.

First inspect the existing payment/booking tables/models.

If existing fields are Stripe-specific, determine whether they can safely be renamed/generalized.

Prefer provider-neutral fields such as:

paymentProvider
providerOrderId
providerPaymentId
providerRefundId
paymentStatus
amount
currency
metadata

Do not perform destructive migrations.

If a migration is genuinely required, create a safe migration and explain exactly what changes.

12. PROVIDER-REFERENCE UNIQUENESS

Implement database-level protection against duplicate provider references where practical.

At minimum, investigate uniqueness for:

payment provider + provider order ID
payment provider + provider payment ID
payment provider + provider refund ID

Do not break existing bookings while implementing this.

13. ADMIN / REPORTING

Check existing admin/vendor/customer payment reports.

They should continue to show:
- booking
- customer
- amount
- currency
- payment provider
- payment status
- payment ID/order ID
- refund status where applicable
- timestamps

Do not hard-code the UI to display "Stripe".

Use a generic payment-provider label.

14. TEST MODE / SANDBOX

Implement Razorpay test-mode support first.

The application must clearly distinguish:

development/test
production

Never mix test credentials and production credentials.

Do not use fake payment-success logic in production.

If demo authentication exists, keep the existing demo-auth restrictions exactly as they are.

15. ERROR HANDLING

Add safe error handling for:

- Razorpay unavailable
- order creation failure
- checkout cancellation
- payment failure
- invalid signature
- duplicate payment
- duplicate webhook
- expired booking
- amount mismatch
- currency mismatch
- invalid booking
- unauthorized payment attempt
- refund failure

Do not expose:
- API secrets
- webhook secrets
- internal stack traces
- database errors
- provider credentials

to the mobile client.

16. DO NOT CHANGE UNRELATED FEATURES

Do not modify:
- Clerk authentication
- customer/vendor/admin roles
- organization removal work already completed
- CORS security hardening
- demo-auth restrictions
- application-status rate limiting
- vendor application rate limiting
- existing booking behavior
- existing database data
- existing UI unnecessarily

Only change payment integration and the minimum required supporting code.

17. SEARCH BEFORE MODIFYING

Before implementation, inspect:

- package.json
- Expo configuration
- app.json/app.config.*
- backend payment routes
- booking routes/services
- payment database models
- webhook routes
- mobile checkout screens
- environment configuration
- existing payment tests

Then provide a short implementation plan.

After the plan, implement the changes.

18. TESTING REQUIREMENTS

Run:

- all existing API tests
- payment-specific tests
- booking tests
- webhook tests
- authentication tests
- TypeScript typecheck
- lint if configured
- git diff --check

Add tests for:

A. Successful Razorpay order creation
B. Invalid credentials/configuration
C. Invalid payment signature
D. Valid payment verification
E. Amount mismatch
F. Currency mismatch
G. Wrong booking/customer
H. Duplicate payment
I. Duplicate webhook
J. Invalid webhook signature
K. Refund authorization
L. Duplicate refund
M. Provider failure
N. Application startup without Stripe credentials

Do NOT claim real Razorpay payment success unless it was actually tested against Razorpay test/sandbox infrastructure.

19. STARTUP REQUIREMENT

After implementation, restart the API.

Confirm that:

- migrations complete
- API starts
- Razorpay configuration is loaded without exposing secrets
- server listens on port 8080
- no Stripe credential is required
- no payment initialization error occurs because Stripe is absent

20. FINAL REPORT

At the end, provide:

1. Files changed
2. Stripe references removed/isolated
3. Razorpay implementation details
4. Environment variables required
5. Mobile integration requirements
6. Database changes, if any
7. Webhook implementation
8. Security protections
9. Tests executed and exact pass/fail counts
10. Whether the application can start without Stripe
11. Exact remaining manual steps required to connect Razorpay test credentials
12. Any Expo/EAS development-build requirement
13. Any limitations that must be addressed before production

IMPORTANT:

Do not ask me to connect Stripe.

Do not require Stripe for startup.

Do not create fake Razorpay credentials.

Do not put credentials in source code.

Do not weaken existing payment security.

Use Razorpay test mode first and keep the implementation production-ready.