Perform a **READ-ONLY AUDIT of the Expo Web authentication/API session flow**.

IMPORTANT:

* Do NOT modify any files.
* Do NOT fix anything.
* Do NOT change Clerk configuration.
* Do NOT change environment variables or secrets.
* Do NOT change authentication architecture.
* Do NOT implement Email OTP.
* Do NOT implement Phone OTP.
* Do NOT change native Android/iOS authentication.
* Do NOT change the database.
* Do NOT change backend authorization.

We are investigating ONE specific finding from the previous login audit:

> Expo Web may fail to authenticate protected API requests when the frontend and API are cross-origin because the web client does not send a Clerk bearer token and `customFetch` does not explicitly send credentials.

I need you to determine whether this is an ACTUAL problem in the current deployment/configuration or only a theoretical risk.

## 1. TRACE THE WEB AUTH FLOW

Inspect:

* `artifacts/travel-land-app/app/_layout.tsx`
* `lib/api-client-react/src/custom-fetch.ts`
* ClerkProvider configuration
* Clerk token cache
* API client initialization
* API base URL configuration
* Web-specific authentication logic

Determine exactly:

WEB:

Clerk session
→ token retrieval
→ API request
→ authentication header/cookie
→ backend Clerk middleware
→ `/v1/me`

Document every step.

## 2. CHECK WEB TOKEN BEHAVIOR

Determine:

* Does web call Clerk `getToken()`?
* Is the bearer token intentionally disabled on web?
* Why?
* Does the browser receive Clerk authentication cookies?
* Are those cookies first-party or cross-origin?
* Is the API on the same origin as the Expo web application?
* If cross-origin, are cookies configured to work?
* Does the API require credentials?
* Does CORS allow credentials?

Do not expose actual tokens, cookies, secrets, or credentials.

## 3. CHECK API BASE URL

Identify the actual API URL configuration used by Expo Web.

Report:

FRONTEND ORIGIN:
API ORIGIN:

Do not expose secrets.

Determine whether they are:

* same-origin
* same-site
* cross-origin

## 4. INSPECT CUSTOM FETCH

Inspect the exact behavior around:

* Authorization header
* `credentials`
* cookies
* web
* native
* 401 handling
* Clerk token retrieval

Determine whether this statement is true:

"On cross-origin Expo Web, protected API requests can reach the API without Clerk authentication."

Do not assume. Prove or disprove it from the current code/configuration.

## 5. INSPECT BACKEND CORS

Inspect:

`artifacts/api-server/src/app.ts`

and any CORS configuration.

Determine:

* allowed origins
* credentials setting
* allowed headers
* Authorization header support
* Clerk middleware ordering
* whether cross-origin requests can authenticate correctly

## 6. TRACE `/v1/me`

Trace the complete request.

Determine:

Web Login
→ Clerk session
→ `/v1/me`
→ `requireAuth`
→ Clerk userId
→ local user
→ role
→ RoleContext
→ authenticated application

Identify exactly where authentication could fail.

## 7. CHECK CURRENT TEST COVERAGE

Determine whether there are tests for:

* web authenticated API request
* cross-origin API request
* Clerk cookie authentication
* bearer token authentication
* credentials include
* `/v1/me` after web login
* web 401 handling

Do not add tests yet.

## 8. DO NOT FIX ANYTHING

This is an audit only.

If you find an issue, report the smallest safe fix but do not implement it.

## REQUIRED FINAL REPORT

### A. WEB AUTH FLOW

Show:

`Clerk → Token/Cookie → Fetch → API → Clerk middleware → /v1/me`

and explain each step.

### B. CURRENT CONFIGURATION

* Frontend origin:
* API origin:
* Same-origin: YES/NO
* Cross-origin: YES/NO
* Web bearer token: YES/NO
* Web cookies: YES/NO
* `credentials: include`: YES/NO
* CORS credentials: YES/NO

Do not expose secrets.

### C. FINDING

Choose exactly one:

**PASS — Web authentication is correctly configured**

or

**FAIL — Web authenticated API requests can fail**

or

**UNVERIFIED — Configuration/runtime evidence is insufficient**

### D. ROOT CAUSE

If FAIL, identify the exact file/function and explain why.

### E. IMPACT

Explain whether this affects:

* Login
* `/v1/me`
* Role routing
* Home screen
* Protected API calls
* Logout
* Native Android/iOS

### F. EXACT FIX

Give the minimum safe fix required.

Do NOT implement it.

### G. TEST PLAN

Give the exact tests that should be run after the fix.

IMPORTANT:
Do not modify anything during this audit.
Do not claim PASS without sufficient evidence.
