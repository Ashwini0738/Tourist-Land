Perform a **READ-ONLY AUDIT of the Login / Authentication flow** in the current Tourism / Travel & Land Expo application.

IMPORTANT:

* This is AUDIT ONLY.
* Do NOT modify any files.
* Do NOT fix anything yet.
* Do NOT install/update/downgrade packages.
* Do NOT change Clerk configuration.
* Do NOT change environment variables or secrets.
* Do NOT implement Phone OTP.
* Do NOT implement Email OTP.
* Do NOT create a custom OTP system.
* Do NOT change the existing authentication flow.

I will use your audit results to create a separate fix task afterward.

## 1. AUDIT CURRENT LOGIN UI

Inspect the complete login screen and identify:

* Login screen location/file
* Email field
* Password field
* Login button
* Forgot password
* Registration link
* Loading state
* Error messages
* Validation
* Keyboard behavior
* Navigation
* Any authentication-method selector already present

Report whether each works correctly.

## 2. AUDIT CLERK SIGN-IN

Inspect the actual implementation of:

* ClerkProvider
* useSignIn()
* signIn.create()
* signIn.attemptFirstFactor()
* signIn.attemptSecondFactor()
* session activation
* Clerk error handling
* authentication state

Determine exactly how the current login works.

DO NOT assume the API is correct just because TypeScript compiles.

## 3. TEST CURRENT EMAIL/PASSWORD LOGIN

If a safe development/test account already exists, test the existing login flow.

Check:

* Valid credentials
* Invalid email
* Invalid password
* Empty email
* Empty password
* Invalid email format
* Loading state
* Network/API failure
* Clerk error
* Successful authentication

Do NOT create or modify accounts.

If testing cannot be performed because credentials are unavailable, clearly state:

TEST BLOCKED — NO TEST ACCOUNT PROVIDED

Do not ask for passwords or credentials.

## 4. AUDIT SESSION CREATION

After successful authentication, verify:

* Clerk session is created
* Session becomes active
* User is available through Clerk
* App does not immediately redirect back to login
* Session persists after navigation
* Session restoration works after app restart if testable

## 5. AUDIT ROLE ROUTING

Inspect what happens after login.

Verify routing for:

* Traveller
* Vendor
* Admin

Check that the app uses the existing Clerk/user role mapping correctly.

Look for:

* incorrect redirects
* infinite redirects
* missing routes
* unauthorized access
* race conditions
* loading-state problems

## 6. AUDIT BACKEND AUTHENTICATION

Inspect how the authenticated Clerk session/token is passed to the backend.

Check:

* Bearer token generation
* API request authentication
* 401 handling
* 403 handling
* unauthorized redirect/logout
* user synchronization

Do NOT modify backend code.

## 7. AUDIT BIOMETRIC / DEVICE SECURITY

Inspect how login interacts with the existing:

* biometric authentication
* passcode/device unlock
* expo-secure-store
* returning-user session restoration

Determine whether biometric authentication is:

A. Initial server authentication
B. Returning-user local unlock
C. Incorrectly mixed with Clerk authentication

Do not modify it.

## 8. AUDIT REGISTRATION RELATIONSHIP

Inspect whether login correctly works with users created through the existing registration flow.

Check:

* Email verification
* Verified/unverified users
* Existing Clerk users
* PostgreSQL user records
* Role mapping

Do not modify anything.

## 9. CHECK FOR CURRENT ERRORS

Search the project for login-related:

* TypeScript errors
* runtime errors
* navigation errors
* Clerk errors
* API errors
* undefined/null errors
* incorrect imports
* deprecated Clerk APIs
* Expo compatibility problems

Also inspect recent logs if available.

## 10. IMPORTANT — DETERMINE WHAT IS ACTUALLY MISSING

At the end, compare the current implementation with this desired future login architecture:

CURRENT:

Email + Password
→ Clerk
→ Session
→ Role routing
→ App

DESIRED FUTURE:

OPTION 1:
Email
→ Verification Code
→ Clerk
→ Session
→ Role routing
→ App

OPTION 2:
Phone
→ SMS OTP
→ Clerk
→ Session
→ Role routing
→ App

Do NOT implement either option.

Only report what is currently possible and what is missing.

## REQUIRED FINAL REPORT

Return the results in exactly this structure:

### A. LOGIN AUDIT

* Login UI: PASS/FAIL
* Email validation: PASS/FAIL
* Password validation: PASS/FAIL
* Login button: PASS/FAIL
* Error handling: PASS/FAIL
* Loading state: PASS/FAIL

### B. CLERK

* ClerkProvider: PASS/FAIL
* useSignIn: PASS/FAIL
* Sign-in API: PASS/FAIL
* Session activation: PASS/FAIL
* Session restoration: PASS/FAIL
* Clerk error handling: PASS/FAIL

### C. ROLE ROUTING

* Traveller: PASS/FAIL
* Vendor: PASS/FAIL
* Admin: PASS/FAIL

### D. BACKEND

* Clerk bearer token: PASS/FAIL
* API authentication: PASS/FAIL
* 401 handling: PASS/FAIL
* 403 handling: PASS/FAIL

### E. BIOMETRIC / DEVICE SECURITY

* Biometric flow: PASS/FAIL
* Passcode flow: PASS/FAIL
* Secure storage: PASS/FAIL
* Returning-user unlock: PASS/FAIL

### F. REGISTRATION COMPATIBILITY

* Email verification: PASS/FAIL
* Existing users: PASS/FAIL
* User/role synchronization: PASS/FAIL

### G. ERRORS FOUND

For every error provide:

1. Error:
2. Root cause:
3. File:
4. Line/function:
5. Severity:
6. Blocking login: YES/NO

### H. MISSING FUNCTIONALITY

Clearly separate:

* Already implemented
* Broken
* Missing
* Blocked by Clerk/Replit

### I. EXACT FIX ORDER

Give the safest order in which the problems should be fixed.

IMPORTANT:
Do not make any changes during this audit.
Do not claim PASS unless you actually verified it.
